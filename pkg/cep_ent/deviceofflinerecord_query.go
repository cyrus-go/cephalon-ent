// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/device"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/deviceofflinerecord"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
)

// DeviceOfflineRecordQuery is the builder for querying DeviceOfflineRecord entities.
type DeviceOfflineRecordQuery struct {
	config
	ctx        *QueryContext
	order      []deviceofflinerecord.OrderOption
	inters     []Interceptor
	predicates []predicate.DeviceOfflineRecord
	withDevice *DeviceQuery
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeviceOfflineRecordQuery builder.
func (dorq *DeviceOfflineRecordQuery) Where(ps ...predicate.DeviceOfflineRecord) *DeviceOfflineRecordQuery {
	dorq.predicates = append(dorq.predicates, ps...)
	return dorq
}

// Limit the number of records to be returned by this query.
func (dorq *DeviceOfflineRecordQuery) Limit(limit int) *DeviceOfflineRecordQuery {
	dorq.ctx.Limit = &limit
	return dorq
}

// Offset to start from.
func (dorq *DeviceOfflineRecordQuery) Offset(offset int) *DeviceOfflineRecordQuery {
	dorq.ctx.Offset = &offset
	return dorq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dorq *DeviceOfflineRecordQuery) Unique(unique bool) *DeviceOfflineRecordQuery {
	dorq.ctx.Unique = &unique
	return dorq
}

// Order specifies how the records should be ordered.
func (dorq *DeviceOfflineRecordQuery) Order(o ...deviceofflinerecord.OrderOption) *DeviceOfflineRecordQuery {
	dorq.order = append(dorq.order, o...)
	return dorq
}

// QueryDevice chains the current query on the "device" edge.
func (dorq *DeviceOfflineRecordQuery) QueryDevice() *DeviceQuery {
	query := (&DeviceClient{config: dorq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dorq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dorq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deviceofflinerecord.Table, deviceofflinerecord.FieldID, selector),
			sqlgraph.To(device.Table, device.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, deviceofflinerecord.DeviceTable, deviceofflinerecord.DeviceColumn),
		)
		fromU = sqlgraph.SetNeighbors(dorq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DeviceOfflineRecord entity from the query.
// Returns a *NotFoundError when no DeviceOfflineRecord was found.
func (dorq *DeviceOfflineRecordQuery) First(ctx context.Context) (*DeviceOfflineRecord, error) {
	nodes, err := dorq.Limit(1).All(setContextOp(ctx, dorq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deviceofflinerecord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) FirstX(ctx context.Context) *DeviceOfflineRecord {
	node, err := dorq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeviceOfflineRecord ID from the query.
// Returns a *NotFoundError when no DeviceOfflineRecord ID was found.
func (dorq *DeviceOfflineRecordQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dorq.Limit(1).IDs(setContextOp(ctx, dorq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deviceofflinerecord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) FirstIDX(ctx context.Context) int64 {
	id, err := dorq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeviceOfflineRecord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeviceOfflineRecord entity is found.
// Returns a *NotFoundError when no DeviceOfflineRecord entities are found.
func (dorq *DeviceOfflineRecordQuery) Only(ctx context.Context) (*DeviceOfflineRecord, error) {
	nodes, err := dorq.Limit(2).All(setContextOp(ctx, dorq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deviceofflinerecord.Label}
	default:
		return nil, &NotSingularError{deviceofflinerecord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) OnlyX(ctx context.Context) *DeviceOfflineRecord {
	node, err := dorq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeviceOfflineRecord ID in the query.
// Returns a *NotSingularError when more than one DeviceOfflineRecord ID is found.
// Returns a *NotFoundError when no entities are found.
func (dorq *DeviceOfflineRecordQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dorq.Limit(2).IDs(setContextOp(ctx, dorq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deviceofflinerecord.Label}
	default:
		err = &NotSingularError{deviceofflinerecord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := dorq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeviceOfflineRecords.
func (dorq *DeviceOfflineRecordQuery) All(ctx context.Context) ([]*DeviceOfflineRecord, error) {
	ctx = setContextOp(ctx, dorq.ctx, "All")
	if err := dorq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeviceOfflineRecord, *DeviceOfflineRecordQuery]()
	return withInterceptors[[]*DeviceOfflineRecord](ctx, dorq, qr, dorq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) AllX(ctx context.Context) []*DeviceOfflineRecord {
	nodes, err := dorq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeviceOfflineRecord IDs.
func (dorq *DeviceOfflineRecordQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if dorq.ctx.Unique == nil && dorq.path != nil {
		dorq.Unique(true)
	}
	ctx = setContextOp(ctx, dorq.ctx, "IDs")
	if err = dorq.Select(deviceofflinerecord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) IDsX(ctx context.Context) []int64 {
	ids, err := dorq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dorq *DeviceOfflineRecordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dorq.ctx, "Count")
	if err := dorq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dorq, querierCount[*DeviceOfflineRecordQuery](), dorq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) CountX(ctx context.Context) int {
	count, err := dorq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dorq *DeviceOfflineRecordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dorq.ctx, "Exist")
	switch _, err := dorq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dorq *DeviceOfflineRecordQuery) ExistX(ctx context.Context) bool {
	exist, err := dorq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeviceOfflineRecordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dorq *DeviceOfflineRecordQuery) Clone() *DeviceOfflineRecordQuery {
	if dorq == nil {
		return nil
	}
	return &DeviceOfflineRecordQuery{
		config:     dorq.config,
		ctx:        dorq.ctx.Clone(),
		order:      append([]deviceofflinerecord.OrderOption{}, dorq.order...),
		inters:     append([]Interceptor{}, dorq.inters...),
		predicates: append([]predicate.DeviceOfflineRecord{}, dorq.predicates...),
		withDevice: dorq.withDevice.Clone(),
		// clone intermediate query.
		sql:  dorq.sql.Clone(),
		path: dorq.path,
	}
}

// WithDevice tells the query-builder to eager-load the nodes that are connected to
// the "device" edge. The optional arguments are used to configure the query builder of the edge.
func (dorq *DeviceOfflineRecordQuery) WithDevice(opts ...func(*DeviceQuery)) *DeviceOfflineRecordQuery {
	query := (&DeviceClient{config: dorq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dorq.withDevice = query
	return dorq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DeviceOfflineRecord.Query().
//		GroupBy(deviceofflinerecord.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (dorq *DeviceOfflineRecordQuery) GroupBy(field string, fields ...string) *DeviceOfflineRecordGroupBy {
	dorq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeviceOfflineRecordGroupBy{build: dorq}
	grbuild.flds = &dorq.ctx.Fields
	grbuild.label = deviceofflinerecord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//	}
//
//	client.DeviceOfflineRecord.Query().
//		Select(deviceofflinerecord.FieldCreatedBy).
//		Scan(ctx, &v)
func (dorq *DeviceOfflineRecordQuery) Select(fields ...string) *DeviceOfflineRecordSelect {
	dorq.ctx.Fields = append(dorq.ctx.Fields, fields...)
	sbuild := &DeviceOfflineRecordSelect{DeviceOfflineRecordQuery: dorq}
	sbuild.label = deviceofflinerecord.Label
	sbuild.flds, sbuild.scan = &dorq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeviceOfflineRecordSelect configured with the given aggregations.
func (dorq *DeviceOfflineRecordQuery) Aggregate(fns ...AggregateFunc) *DeviceOfflineRecordSelect {
	return dorq.Select().Aggregate(fns...)
}

func (dorq *DeviceOfflineRecordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dorq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dorq); err != nil {
				return err
			}
		}
	}
	for _, f := range dorq.ctx.Fields {
		if !deviceofflinerecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if dorq.path != nil {
		prev, err := dorq.path(ctx)
		if err != nil {
			return err
		}
		dorq.sql = prev
	}
	return nil
}

func (dorq *DeviceOfflineRecordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeviceOfflineRecord, error) {
	var (
		nodes       = []*DeviceOfflineRecord{}
		_spec       = dorq.querySpec()
		loadedTypes = [1]bool{
			dorq.withDevice != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeviceOfflineRecord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeviceOfflineRecord{config: dorq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(dorq.modifiers) > 0 {
		_spec.Modifiers = dorq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dorq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dorq.withDevice; query != nil {
		if err := dorq.loadDevice(ctx, query, nodes, nil,
			func(n *DeviceOfflineRecord, e *Device) { n.Edges.Device = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dorq *DeviceOfflineRecordQuery) loadDevice(ctx context.Context, query *DeviceQuery, nodes []*DeviceOfflineRecord, init func(*DeviceOfflineRecord), assign func(*DeviceOfflineRecord, *Device)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*DeviceOfflineRecord)
	for i := range nodes {
		fk := nodes[i].DeviceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(device.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "device_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dorq *DeviceOfflineRecordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dorq.querySpec()
	if len(dorq.modifiers) > 0 {
		_spec.Modifiers = dorq.modifiers
	}
	_spec.Node.Columns = dorq.ctx.Fields
	if len(dorq.ctx.Fields) > 0 {
		_spec.Unique = dorq.ctx.Unique != nil && *dorq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dorq.driver, _spec)
}

func (dorq *DeviceOfflineRecordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(deviceofflinerecord.Table, deviceofflinerecord.Columns, sqlgraph.NewFieldSpec(deviceofflinerecord.FieldID, field.TypeInt64))
	_spec.From = dorq.sql
	if unique := dorq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dorq.path != nil {
		_spec.Unique = true
	}
	if fields := dorq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deviceofflinerecord.FieldID)
		for i := range fields {
			if fields[i] != deviceofflinerecord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dorq.withDevice != nil {
			_spec.Node.AddColumnOnce(deviceofflinerecord.FieldDeviceID)
		}
	}
	if ps := dorq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dorq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dorq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dorq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dorq *DeviceOfflineRecordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dorq.driver.Dialect())
	t1 := builder.Table(deviceofflinerecord.Table)
	columns := dorq.ctx.Fields
	if len(columns) == 0 {
		columns = deviceofflinerecord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dorq.sql != nil {
		selector = dorq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dorq.ctx.Unique != nil && *dorq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range dorq.modifiers {
		m(selector)
	}
	for _, p := range dorq.predicates {
		p(selector)
	}
	for _, p := range dorq.order {
		p(selector)
	}
	if offset := dorq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dorq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dorq *DeviceOfflineRecordQuery) Modify(modifiers ...func(s *sql.Selector)) *DeviceOfflineRecordSelect {
	dorq.modifiers = append(dorq.modifiers, modifiers...)
	return dorq.Select()
}

// DeviceOfflineRecordGroupBy is the group-by builder for DeviceOfflineRecord entities.
type DeviceOfflineRecordGroupBy struct {
	selector
	build *DeviceOfflineRecordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dorgb *DeviceOfflineRecordGroupBy) Aggregate(fns ...AggregateFunc) *DeviceOfflineRecordGroupBy {
	dorgb.fns = append(dorgb.fns, fns...)
	return dorgb
}

// Scan applies the selector query and scans the result into the given value.
func (dorgb *DeviceOfflineRecordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dorgb.build.ctx, "GroupBy")
	if err := dorgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeviceOfflineRecordQuery, *DeviceOfflineRecordGroupBy](ctx, dorgb.build, dorgb, dorgb.build.inters, v)
}

func (dorgb *DeviceOfflineRecordGroupBy) sqlScan(ctx context.Context, root *DeviceOfflineRecordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dorgb.fns))
	for _, fn := range dorgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dorgb.flds)+len(dorgb.fns))
		for _, f := range *dorgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dorgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dorgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeviceOfflineRecordSelect is the builder for selecting fields of DeviceOfflineRecord entities.
type DeviceOfflineRecordSelect struct {
	*DeviceOfflineRecordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dors *DeviceOfflineRecordSelect) Aggregate(fns ...AggregateFunc) *DeviceOfflineRecordSelect {
	dors.fns = append(dors.fns, fns...)
	return dors
}

// Scan applies the selector query and scans the result into the given value.
func (dors *DeviceOfflineRecordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dors.ctx, "Select")
	if err := dors.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeviceOfflineRecordQuery, *DeviceOfflineRecordSelect](ctx, dors.DeviceOfflineRecordQuery, dors, dors.inters, v)
}

func (dors *DeviceOfflineRecordSelect) sqlScan(ctx context.Context, root *DeviceOfflineRecordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dors.fns))
	for _, fn := range dors.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dors.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dors.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (dors *DeviceOfflineRecordSelect) Modify(modifiers ...func(s *sql.Selector)) *DeviceOfflineRecordSelect {
	dors.modifiers = append(dors.modifiers, modifiers...)
	return dors
}

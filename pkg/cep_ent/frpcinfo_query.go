// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/device"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/frpcinfo"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/frpsinfo"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
)

// FrpcInfoQuery is the builder for querying FrpcInfo entities.
type FrpcInfoQuery struct {
	config
	ctx          *QueryContext
	order        []frpcinfo.OrderOption
	inters       []Interceptor
	predicates   []predicate.FrpcInfo
	withFrpsInfo *FrpsInfoQuery
	withDevice   *DeviceQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FrpcInfoQuery builder.
func (fiq *FrpcInfoQuery) Where(ps ...predicate.FrpcInfo) *FrpcInfoQuery {
	fiq.predicates = append(fiq.predicates, ps...)
	return fiq
}

// Limit the number of records to be returned by this query.
func (fiq *FrpcInfoQuery) Limit(limit int) *FrpcInfoQuery {
	fiq.ctx.Limit = &limit
	return fiq
}

// Offset to start from.
func (fiq *FrpcInfoQuery) Offset(offset int) *FrpcInfoQuery {
	fiq.ctx.Offset = &offset
	return fiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fiq *FrpcInfoQuery) Unique(unique bool) *FrpcInfoQuery {
	fiq.ctx.Unique = &unique
	return fiq
}

// Order specifies how the records should be ordered.
func (fiq *FrpcInfoQuery) Order(o ...frpcinfo.OrderOption) *FrpcInfoQuery {
	fiq.order = append(fiq.order, o...)
	return fiq
}

// QueryFrpsInfo chains the current query on the "frps_info" edge.
func (fiq *FrpcInfoQuery) QueryFrpsInfo() *FrpsInfoQuery {
	query := (&FrpsInfoClient{config: fiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(frpcinfo.Table, frpcinfo.FieldID, selector),
			sqlgraph.To(frpsinfo.Table, frpsinfo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, frpcinfo.FrpsInfoTable, frpcinfo.FrpsInfoColumn),
		)
		fromU = sqlgraph.SetNeighbors(fiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryDevice chains the current query on the "device" edge.
func (fiq *FrpcInfoQuery) QueryDevice() *DeviceQuery {
	query := (&DeviceClient{config: fiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(frpcinfo.Table, frpcinfo.FieldID, selector),
			sqlgraph.To(device.Table, device.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, frpcinfo.DeviceTable, frpcinfo.DeviceColumn),
		)
		fromU = sqlgraph.SetNeighbors(fiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FrpcInfo entity from the query.
// Returns a *NotFoundError when no FrpcInfo was found.
func (fiq *FrpcInfoQuery) First(ctx context.Context) (*FrpcInfo, error) {
	nodes, err := fiq.Limit(1).All(setContextOp(ctx, fiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{frpcinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fiq *FrpcInfoQuery) FirstX(ctx context.Context) *FrpcInfo {
	node, err := fiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FrpcInfo ID from the query.
// Returns a *NotFoundError when no FrpcInfo ID was found.
func (fiq *FrpcInfoQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fiq.Limit(1).IDs(setContextOp(ctx, fiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{frpcinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fiq *FrpcInfoQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FrpcInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FrpcInfo entity is found.
// Returns a *NotFoundError when no FrpcInfo entities are found.
func (fiq *FrpcInfoQuery) Only(ctx context.Context) (*FrpcInfo, error) {
	nodes, err := fiq.Limit(2).All(setContextOp(ctx, fiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{frpcinfo.Label}
	default:
		return nil, &NotSingularError{frpcinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fiq *FrpcInfoQuery) OnlyX(ctx context.Context) *FrpcInfo {
	node, err := fiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FrpcInfo ID in the query.
// Returns a *NotSingularError when more than one FrpcInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (fiq *FrpcInfoQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fiq.Limit(2).IDs(setContextOp(ctx, fiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{frpcinfo.Label}
	default:
		err = &NotSingularError{frpcinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fiq *FrpcInfoQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FrpcInfos.
func (fiq *FrpcInfoQuery) All(ctx context.Context) ([]*FrpcInfo, error) {
	ctx = setContextOp(ctx, fiq.ctx, "All")
	if err := fiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FrpcInfo, *FrpcInfoQuery]()
	return withInterceptors[[]*FrpcInfo](ctx, fiq, qr, fiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fiq *FrpcInfoQuery) AllX(ctx context.Context) []*FrpcInfo {
	nodes, err := fiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FrpcInfo IDs.
func (fiq *FrpcInfoQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fiq.ctx.Unique == nil && fiq.path != nil {
		fiq.Unique(true)
	}
	ctx = setContextOp(ctx, fiq.ctx, "IDs")
	if err = fiq.Select(frpcinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fiq *FrpcInfoQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fiq *FrpcInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Count")
	if err := fiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fiq, querierCount[*FrpcInfoQuery](), fiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fiq *FrpcInfoQuery) CountX(ctx context.Context) int {
	count, err := fiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fiq *FrpcInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fiq.ctx, "Exist")
	switch _, err := fiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fiq *FrpcInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := fiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FrpcInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fiq *FrpcInfoQuery) Clone() *FrpcInfoQuery {
	if fiq == nil {
		return nil
	}
	return &FrpcInfoQuery{
		config:       fiq.config,
		ctx:          fiq.ctx.Clone(),
		order:        append([]frpcinfo.OrderOption{}, fiq.order...),
		inters:       append([]Interceptor{}, fiq.inters...),
		predicates:   append([]predicate.FrpcInfo{}, fiq.predicates...),
		withFrpsInfo: fiq.withFrpsInfo.Clone(),
		withDevice:   fiq.withDevice.Clone(),
		// clone intermediate query.
		sql:  fiq.sql.Clone(),
		path: fiq.path,
	}
}

// WithFrpsInfo tells the query-builder to eager-load the nodes that are connected to
// the "frps_info" edge. The optional arguments are used to configure the query builder of the edge.
func (fiq *FrpcInfoQuery) WithFrpsInfo(opts ...func(*FrpsInfoQuery)) *FrpcInfoQuery {
	query := (&FrpsInfoClient{config: fiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fiq.withFrpsInfo = query
	return fiq
}

// WithDevice tells the query-builder to eager-load the nodes that are connected to
// the "device" edge. The optional arguments are used to configure the query builder of the edge.
func (fiq *FrpcInfoQuery) WithDevice(opts ...func(*DeviceQuery)) *FrpcInfoQuery {
	query := (&DeviceClient{config: fiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fiq.withDevice = query
	return fiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FrpcInfo.Query().
//		GroupBy(frpcinfo.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (fiq *FrpcInfoQuery) GroupBy(field string, fields ...string) *FrpcInfoGroupBy {
	fiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FrpcInfoGroupBy{build: fiq}
	grbuild.flds = &fiq.ctx.Fields
	grbuild.label = frpcinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//	}
//
//	client.FrpcInfo.Query().
//		Select(frpcinfo.FieldCreatedBy).
//		Scan(ctx, &v)
func (fiq *FrpcInfoQuery) Select(fields ...string) *FrpcInfoSelect {
	fiq.ctx.Fields = append(fiq.ctx.Fields, fields...)
	sbuild := &FrpcInfoSelect{FrpcInfoQuery: fiq}
	sbuild.label = frpcinfo.Label
	sbuild.flds, sbuild.scan = &fiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FrpcInfoSelect configured with the given aggregations.
func (fiq *FrpcInfoQuery) Aggregate(fns ...AggregateFunc) *FrpcInfoSelect {
	return fiq.Select().Aggregate(fns...)
}

func (fiq *FrpcInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fiq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fiq); err != nil {
				return err
			}
		}
	}
	for _, f := range fiq.ctx.Fields {
		if !frpcinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if fiq.path != nil {
		prev, err := fiq.path(ctx)
		if err != nil {
			return err
		}
		fiq.sql = prev
	}
	return nil
}

func (fiq *FrpcInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FrpcInfo, error) {
	var (
		nodes       = []*FrpcInfo{}
		_spec       = fiq.querySpec()
		loadedTypes = [2]bool{
			fiq.withFrpsInfo != nil,
			fiq.withDevice != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FrpcInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FrpcInfo{config: fiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fiq.withFrpsInfo; query != nil {
		if err := fiq.loadFrpsInfo(ctx, query, nodes, nil,
			func(n *FrpcInfo, e *FrpsInfo) { n.Edges.FrpsInfo = e }); err != nil {
			return nil, err
		}
	}
	if query := fiq.withDevice; query != nil {
		if err := fiq.loadDevice(ctx, query, nodes, nil,
			func(n *FrpcInfo, e *Device) { n.Edges.Device = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fiq *FrpcInfoQuery) loadFrpsInfo(ctx context.Context, query *FrpsInfoQuery, nodes []*FrpcInfo, init func(*FrpcInfo), assign func(*FrpcInfo, *FrpsInfo)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*FrpcInfo)
	for i := range nodes {
		fk := nodes[i].FrpsID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(frpsinfo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "frps_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (fiq *FrpcInfoQuery) loadDevice(ctx context.Context, query *DeviceQuery, nodes []*FrpcInfo, init func(*FrpcInfo), assign func(*FrpcInfo, *Device)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*FrpcInfo)
	for i := range nodes {
		fk := nodes[i].DeviceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(device.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "device_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fiq *FrpcInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fiq.querySpec()
	_spec.Node.Columns = fiq.ctx.Fields
	if len(fiq.ctx.Fields) > 0 {
		_spec.Unique = fiq.ctx.Unique != nil && *fiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fiq.driver, _spec)
}

func (fiq *FrpcInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(frpcinfo.Table, frpcinfo.Columns, sqlgraph.NewFieldSpec(frpcinfo.FieldID, field.TypeInt64))
	_spec.From = fiq.sql
	if unique := fiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fiq.path != nil {
		_spec.Unique = true
	}
	if fields := fiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, frpcinfo.FieldID)
		for i := range fields {
			if fields[i] != frpcinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if fiq.withFrpsInfo != nil {
			_spec.Node.AddColumnOnce(frpcinfo.FieldFrpsID)
		}
		if fiq.withDevice != nil {
			_spec.Node.AddColumnOnce(frpcinfo.FieldDeviceID)
		}
	}
	if ps := fiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fiq *FrpcInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fiq.driver.Dialect())
	t1 := builder.Table(frpcinfo.Table)
	columns := fiq.ctx.Fields
	if len(columns) == 0 {
		columns = frpcinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fiq.sql != nil {
		selector = fiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fiq.ctx.Unique != nil && *fiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fiq.predicates {
		p(selector)
	}
	for _, p := range fiq.order {
		p(selector)
	}
	if offset := fiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FrpcInfoGroupBy is the group-by builder for FrpcInfo entities.
type FrpcInfoGroupBy struct {
	selector
	build *FrpcInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (figb *FrpcInfoGroupBy) Aggregate(fns ...AggregateFunc) *FrpcInfoGroupBy {
	figb.fns = append(figb.fns, fns...)
	return figb
}

// Scan applies the selector query and scans the result into the given value.
func (figb *FrpcInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, figb.build.ctx, "GroupBy")
	if err := figb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FrpcInfoQuery, *FrpcInfoGroupBy](ctx, figb.build, figb, figb.build.inters, v)
}

func (figb *FrpcInfoGroupBy) sqlScan(ctx context.Context, root *FrpcInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(figb.fns))
	for _, fn := range figb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*figb.flds)+len(figb.fns))
		for _, f := range *figb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*figb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := figb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FrpcInfoSelect is the builder for selecting fields of FrpcInfo entities.
type FrpcInfoSelect struct {
	*FrpcInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fis *FrpcInfoSelect) Aggregate(fns ...AggregateFunc) *FrpcInfoSelect {
	fis.fns = append(fis.fns, fns...)
	return fis
}

// Scan applies the selector query and scans the result into the given value.
func (fis *FrpcInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fis.ctx, "Select")
	if err := fis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FrpcInfoQuery, *FrpcInfoSelect](ctx, fis.FrpcInfoQuery, fis, fis.inters, v)
}

func (fis *FrpcInfoSelect) sqlScan(ctx context.Context, root *FrpcInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fis.fns))
	for _, fn := range fis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

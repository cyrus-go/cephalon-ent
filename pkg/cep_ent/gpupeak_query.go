// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/gpupeak"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
)

// GpuPeakQuery is the builder for querying GpuPeak entities.
type GpuPeakQuery struct {
	config
	ctx        *QueryContext
	order      []gpupeak.OrderOption
	inters     []Interceptor
	predicates []predicate.GpuPeak
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GpuPeakQuery builder.
func (gpq *GpuPeakQuery) Where(ps ...predicate.GpuPeak) *GpuPeakQuery {
	gpq.predicates = append(gpq.predicates, ps...)
	return gpq
}

// Limit the number of records to be returned by this query.
func (gpq *GpuPeakQuery) Limit(limit int) *GpuPeakQuery {
	gpq.ctx.Limit = &limit
	return gpq
}

// Offset to start from.
func (gpq *GpuPeakQuery) Offset(offset int) *GpuPeakQuery {
	gpq.ctx.Offset = &offset
	return gpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gpq *GpuPeakQuery) Unique(unique bool) *GpuPeakQuery {
	gpq.ctx.Unique = &unique
	return gpq
}

// Order specifies how the records should be ordered.
func (gpq *GpuPeakQuery) Order(o ...gpupeak.OrderOption) *GpuPeakQuery {
	gpq.order = append(gpq.order, o...)
	return gpq
}

// First returns the first GpuPeak entity from the query.
// Returns a *NotFoundError when no GpuPeak was found.
func (gpq *GpuPeakQuery) First(ctx context.Context) (*GpuPeak, error) {
	nodes, err := gpq.Limit(1).All(setContextOp(ctx, gpq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{gpupeak.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gpq *GpuPeakQuery) FirstX(ctx context.Context) *GpuPeak {
	node, err := gpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GpuPeak ID from the query.
// Returns a *NotFoundError when no GpuPeak ID was found.
func (gpq *GpuPeakQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gpq.Limit(1).IDs(setContextOp(ctx, gpq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{gpupeak.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gpq *GpuPeakQuery) FirstIDX(ctx context.Context) int64 {
	id, err := gpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GpuPeak entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GpuPeak entity is found.
// Returns a *NotFoundError when no GpuPeak entities are found.
func (gpq *GpuPeakQuery) Only(ctx context.Context) (*GpuPeak, error) {
	nodes, err := gpq.Limit(2).All(setContextOp(ctx, gpq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{gpupeak.Label}
	default:
		return nil, &NotSingularError{gpupeak.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gpq *GpuPeakQuery) OnlyX(ctx context.Context) *GpuPeak {
	node, err := gpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GpuPeak ID in the query.
// Returns a *NotSingularError when more than one GpuPeak ID is found.
// Returns a *NotFoundError when no entities are found.
func (gpq *GpuPeakQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = gpq.Limit(2).IDs(setContextOp(ctx, gpq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{gpupeak.Label}
	default:
		err = &NotSingularError{gpupeak.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gpq *GpuPeakQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := gpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GpuPeaks.
func (gpq *GpuPeakQuery) All(ctx context.Context) ([]*GpuPeak, error) {
	ctx = setContextOp(ctx, gpq.ctx, "All")
	if err := gpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GpuPeak, *GpuPeakQuery]()
	return withInterceptors[[]*GpuPeak](ctx, gpq, qr, gpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gpq *GpuPeakQuery) AllX(ctx context.Context) []*GpuPeak {
	nodes, err := gpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GpuPeak IDs.
func (gpq *GpuPeakQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if gpq.ctx.Unique == nil && gpq.path != nil {
		gpq.Unique(true)
	}
	ctx = setContextOp(ctx, gpq.ctx, "IDs")
	if err = gpq.Select(gpupeak.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gpq *GpuPeakQuery) IDsX(ctx context.Context) []int64 {
	ids, err := gpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gpq *GpuPeakQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gpq.ctx, "Count")
	if err := gpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gpq, querierCount[*GpuPeakQuery](), gpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gpq *GpuPeakQuery) CountX(ctx context.Context) int {
	count, err := gpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gpq *GpuPeakQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gpq.ctx, "Exist")
	switch _, err := gpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gpq *GpuPeakQuery) ExistX(ctx context.Context) bool {
	exist, err := gpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GpuPeakQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gpq *GpuPeakQuery) Clone() *GpuPeakQuery {
	if gpq == nil {
		return nil
	}
	return &GpuPeakQuery{
		config:     gpq.config,
		ctx:        gpq.ctx.Clone(),
		order:      append([]gpupeak.OrderOption{}, gpq.order...),
		inters:     append([]Interceptor{}, gpq.inters...),
		predicates: append([]predicate.GpuPeak{}, gpq.predicates...),
		// clone intermediate query.
		sql:  gpq.sql.Clone(),
		path: gpq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GpuPeak.Query().
//		GroupBy(gpupeak.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (gpq *GpuPeakQuery) GroupBy(field string, fields ...string) *GpuPeakGroupBy {
	gpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GpuPeakGroupBy{build: gpq}
	grbuild.flds = &gpq.ctx.Fields
	grbuild.label = gpupeak.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//	}
//
//	client.GpuPeak.Query().
//		Select(gpupeak.FieldCreatedBy).
//		Scan(ctx, &v)
func (gpq *GpuPeakQuery) Select(fields ...string) *GpuPeakSelect {
	gpq.ctx.Fields = append(gpq.ctx.Fields, fields...)
	sbuild := &GpuPeakSelect{GpuPeakQuery: gpq}
	sbuild.label = gpupeak.Label
	sbuild.flds, sbuild.scan = &gpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GpuPeakSelect configured with the given aggregations.
func (gpq *GpuPeakQuery) Aggregate(fns ...AggregateFunc) *GpuPeakSelect {
	return gpq.Select().Aggregate(fns...)
}

func (gpq *GpuPeakQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gpq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gpq); err != nil {
				return err
			}
		}
	}
	for _, f := range gpq.ctx.Fields {
		if !gpupeak.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if gpq.path != nil {
		prev, err := gpq.path(ctx)
		if err != nil {
			return err
		}
		gpq.sql = prev
	}
	return nil
}

func (gpq *GpuPeakQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GpuPeak, error) {
	var (
		nodes = []*GpuPeak{}
		_spec = gpq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GpuPeak).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GpuPeak{config: gpq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(gpq.modifiers) > 0 {
		_spec.Modifiers = gpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (gpq *GpuPeakQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gpq.querySpec()
	if len(gpq.modifiers) > 0 {
		_spec.Modifiers = gpq.modifiers
	}
	_spec.Node.Columns = gpq.ctx.Fields
	if len(gpq.ctx.Fields) > 0 {
		_spec.Unique = gpq.ctx.Unique != nil && *gpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gpq.driver, _spec)
}

func (gpq *GpuPeakQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(gpupeak.Table, gpupeak.Columns, sqlgraph.NewFieldSpec(gpupeak.FieldID, field.TypeInt64))
	_spec.From = gpq.sql
	if unique := gpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gpq.path != nil {
		_spec.Unique = true
	}
	if fields := gpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, gpupeak.FieldID)
		for i := range fields {
			if fields[i] != gpupeak.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gpq *GpuPeakQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gpq.driver.Dialect())
	t1 := builder.Table(gpupeak.Table)
	columns := gpq.ctx.Fields
	if len(columns) == 0 {
		columns = gpupeak.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gpq.sql != nil {
		selector = gpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gpq.ctx.Unique != nil && *gpq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range gpq.modifiers {
		m(selector)
	}
	for _, p := range gpq.predicates {
		p(selector)
	}
	for _, p := range gpq.order {
		p(selector)
	}
	if offset := gpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gpq *GpuPeakQuery) Modify(modifiers ...func(s *sql.Selector)) *GpuPeakSelect {
	gpq.modifiers = append(gpq.modifiers, modifiers...)
	return gpq.Select()
}

// GpuPeakGroupBy is the group-by builder for GpuPeak entities.
type GpuPeakGroupBy struct {
	selector
	build *GpuPeakQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gpgb *GpuPeakGroupBy) Aggregate(fns ...AggregateFunc) *GpuPeakGroupBy {
	gpgb.fns = append(gpgb.fns, fns...)
	return gpgb
}

// Scan applies the selector query and scans the result into the given value.
func (gpgb *GpuPeakGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gpgb.build.ctx, "GroupBy")
	if err := gpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GpuPeakQuery, *GpuPeakGroupBy](ctx, gpgb.build, gpgb, gpgb.build.inters, v)
}

func (gpgb *GpuPeakGroupBy) sqlScan(ctx context.Context, root *GpuPeakQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gpgb.fns))
	for _, fn := range gpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gpgb.flds)+len(gpgb.fns))
		for _, f := range *gpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GpuPeakSelect is the builder for selecting fields of GpuPeak entities.
type GpuPeakSelect struct {
	*GpuPeakQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gps *GpuPeakSelect) Aggregate(fns ...AggregateFunc) *GpuPeakSelect {
	gps.fns = append(gps.fns, fns...)
	return gps
}

// Scan applies the selector query and scans the result into the given value.
func (gps *GpuPeakSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gps.ctx, "Select")
	if err := gps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GpuPeakQuery, *GpuPeakSelect](ctx, gps.GpuPeakQuery, gps, gps.inters, v)
}

func (gps *GpuPeakSelect) sqlScan(ctx context.Context, root *GpuPeakQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gps.fns))
	for _, fn := range gps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (gps *GpuPeakSelect) Modify(modifiers ...func(s *sql.Selector)) *GpuPeakSelect {
	gps.modifiers = append(gps.modifiers, modifiers...)
	return gps
}

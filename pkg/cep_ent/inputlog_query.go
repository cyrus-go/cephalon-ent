// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/inputlog"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
)

// InputLogQuery is the builder for querying InputLog entities.
type InputLogQuery struct {
	config
	ctx        *QueryContext
	order      []inputlog.OrderOption
	inters     []Interceptor
	predicates []predicate.InputLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InputLogQuery builder.
func (ilq *InputLogQuery) Where(ps ...predicate.InputLog) *InputLogQuery {
	ilq.predicates = append(ilq.predicates, ps...)
	return ilq
}

// Limit the number of records to be returned by this query.
func (ilq *InputLogQuery) Limit(limit int) *InputLogQuery {
	ilq.ctx.Limit = &limit
	return ilq
}

// Offset to start from.
func (ilq *InputLogQuery) Offset(offset int) *InputLogQuery {
	ilq.ctx.Offset = &offset
	return ilq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ilq *InputLogQuery) Unique(unique bool) *InputLogQuery {
	ilq.ctx.Unique = &unique
	return ilq
}

// Order specifies how the records should be ordered.
func (ilq *InputLogQuery) Order(o ...inputlog.OrderOption) *InputLogQuery {
	ilq.order = append(ilq.order, o...)
	return ilq
}

// First returns the first InputLog entity from the query.
// Returns a *NotFoundError when no InputLog was found.
func (ilq *InputLogQuery) First(ctx context.Context) (*InputLog, error) {
	nodes, err := ilq.Limit(1).All(setContextOp(ctx, ilq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{inputlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ilq *InputLogQuery) FirstX(ctx context.Context) *InputLog {
	node, err := ilq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InputLog ID from the query.
// Returns a *NotFoundError when no InputLog ID was found.
func (ilq *InputLogQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = ilq.Limit(1).IDs(setContextOp(ctx, ilq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{inputlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ilq *InputLogQuery) FirstIDX(ctx context.Context) int64 {
	id, err := ilq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InputLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InputLog entity is found.
// Returns a *NotFoundError when no InputLog entities are found.
func (ilq *InputLogQuery) Only(ctx context.Context) (*InputLog, error) {
	nodes, err := ilq.Limit(2).All(setContextOp(ctx, ilq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{inputlog.Label}
	default:
		return nil, &NotSingularError{inputlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ilq *InputLogQuery) OnlyX(ctx context.Context) *InputLog {
	node, err := ilq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InputLog ID in the query.
// Returns a *NotSingularError when more than one InputLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (ilq *InputLogQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = ilq.Limit(2).IDs(setContextOp(ctx, ilq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{inputlog.Label}
	default:
		err = &NotSingularError{inputlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ilq *InputLogQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := ilq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InputLogs.
func (ilq *InputLogQuery) All(ctx context.Context) ([]*InputLog, error) {
	ctx = setContextOp(ctx, ilq.ctx, "All")
	if err := ilq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*InputLog, *InputLogQuery]()
	return withInterceptors[[]*InputLog](ctx, ilq, qr, ilq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ilq *InputLogQuery) AllX(ctx context.Context) []*InputLog {
	nodes, err := ilq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InputLog IDs.
func (ilq *InputLogQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if ilq.ctx.Unique == nil && ilq.path != nil {
		ilq.Unique(true)
	}
	ctx = setContextOp(ctx, ilq.ctx, "IDs")
	if err = ilq.Select(inputlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ilq *InputLogQuery) IDsX(ctx context.Context) []int64 {
	ids, err := ilq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ilq *InputLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ilq.ctx, "Count")
	if err := ilq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ilq, querierCount[*InputLogQuery](), ilq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ilq *InputLogQuery) CountX(ctx context.Context) int {
	count, err := ilq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ilq *InputLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ilq.ctx, "Exist")
	switch _, err := ilq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ilq *InputLogQuery) ExistX(ctx context.Context) bool {
	exist, err := ilq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InputLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ilq *InputLogQuery) Clone() *InputLogQuery {
	if ilq == nil {
		return nil
	}
	return &InputLogQuery{
		config:     ilq.config,
		ctx:        ilq.ctx.Clone(),
		order:      append([]inputlog.OrderOption{}, ilq.order...),
		inters:     append([]Interceptor{}, ilq.inters...),
		predicates: append([]predicate.InputLog{}, ilq.predicates...),
		// clone intermediate query.
		sql:  ilq.sql.Clone(),
		path: ilq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InputLog.Query().
//		GroupBy(inputlog.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (ilq *InputLogQuery) GroupBy(field string, fields ...string) *InputLogGroupBy {
	ilq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InputLogGroupBy{build: ilq}
	grbuild.flds = &ilq.ctx.Fields
	grbuild.label = inputlog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//	}
//
//	client.InputLog.Query().
//		Select(inputlog.FieldCreatedBy).
//		Scan(ctx, &v)
func (ilq *InputLogQuery) Select(fields ...string) *InputLogSelect {
	ilq.ctx.Fields = append(ilq.ctx.Fields, fields...)
	sbuild := &InputLogSelect{InputLogQuery: ilq}
	sbuild.label = inputlog.Label
	sbuild.flds, sbuild.scan = &ilq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InputLogSelect configured with the given aggregations.
func (ilq *InputLogQuery) Aggregate(fns ...AggregateFunc) *InputLogSelect {
	return ilq.Select().Aggregate(fns...)
}

func (ilq *InputLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ilq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ilq); err != nil {
				return err
			}
		}
	}
	for _, f := range ilq.ctx.Fields {
		if !inputlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if ilq.path != nil {
		prev, err := ilq.path(ctx)
		if err != nil {
			return err
		}
		ilq.sql = prev
	}
	return nil
}

func (ilq *InputLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InputLog, error) {
	var (
		nodes = []*InputLog{}
		_spec = ilq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*InputLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &InputLog{config: ilq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ilq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ilq *InputLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ilq.querySpec()
	_spec.Node.Columns = ilq.ctx.Fields
	if len(ilq.ctx.Fields) > 0 {
		_spec.Unique = ilq.ctx.Unique != nil && *ilq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ilq.driver, _spec)
}

func (ilq *InputLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(inputlog.Table, inputlog.Columns, sqlgraph.NewFieldSpec(inputlog.FieldID, field.TypeInt64))
	_spec.From = ilq.sql
	if unique := ilq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ilq.path != nil {
		_spec.Unique = true
	}
	if fields := ilq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, inputlog.FieldID)
		for i := range fields {
			if fields[i] != inputlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ilq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ilq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ilq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ilq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ilq *InputLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ilq.driver.Dialect())
	t1 := builder.Table(inputlog.Table)
	columns := ilq.ctx.Fields
	if len(columns) == 0 {
		columns = inputlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ilq.sql != nil {
		selector = ilq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ilq.ctx.Unique != nil && *ilq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ilq.predicates {
		p(selector)
	}
	for _, p := range ilq.order {
		p(selector)
	}
	if offset := ilq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ilq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// InputLogGroupBy is the group-by builder for InputLog entities.
type InputLogGroupBy struct {
	selector
	build *InputLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ilgb *InputLogGroupBy) Aggregate(fns ...AggregateFunc) *InputLogGroupBy {
	ilgb.fns = append(ilgb.fns, fns...)
	return ilgb
}

// Scan applies the selector query and scans the result into the given value.
func (ilgb *InputLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ilgb.build.ctx, "GroupBy")
	if err := ilgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InputLogQuery, *InputLogGroupBy](ctx, ilgb.build, ilgb, ilgb.build.inters, v)
}

func (ilgb *InputLogGroupBy) sqlScan(ctx context.Context, root *InputLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ilgb.fns))
	for _, fn := range ilgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ilgb.flds)+len(ilgb.fns))
		for _, f := range *ilgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ilgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ilgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InputLogSelect is the builder for selecting fields of InputLog entities.
type InputLogSelect struct {
	*InputLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ils *InputLogSelect) Aggregate(fns ...AggregateFunc) *InputLogSelect {
	ils.fns = append(ils.fns, fns...)
	return ils
}

// Scan applies the selector query and scans the result into the given value.
func (ils *InputLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ils.ctx, "Select")
	if err := ils.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InputLogQuery, *InputLogSelect](ctx, ils.InputLogQuery, ils, ils.inters, v)
}

func (ils *InputLogSelect) sqlScan(ctx context.Context, root *InputLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ils.fns))
	for _, fn := range ils.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ils.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ils.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

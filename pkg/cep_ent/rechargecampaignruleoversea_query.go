// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/rechargecampaignruleoversea"
)

// RechargeCampaignRuleOverseaQuery is the builder for querying RechargeCampaignRuleOversea entities.
type RechargeCampaignRuleOverseaQuery struct {
	config
	ctx        *QueryContext
	order      []rechargecampaignruleoversea.OrderOption
	inters     []Interceptor
	predicates []predicate.RechargeCampaignRuleOversea
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RechargeCampaignRuleOverseaQuery builder.
func (rcroq *RechargeCampaignRuleOverseaQuery) Where(ps ...predicate.RechargeCampaignRuleOversea) *RechargeCampaignRuleOverseaQuery {
	rcroq.predicates = append(rcroq.predicates, ps...)
	return rcroq
}

// Limit the number of records to be returned by this query.
func (rcroq *RechargeCampaignRuleOverseaQuery) Limit(limit int) *RechargeCampaignRuleOverseaQuery {
	rcroq.ctx.Limit = &limit
	return rcroq
}

// Offset to start from.
func (rcroq *RechargeCampaignRuleOverseaQuery) Offset(offset int) *RechargeCampaignRuleOverseaQuery {
	rcroq.ctx.Offset = &offset
	return rcroq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rcroq *RechargeCampaignRuleOverseaQuery) Unique(unique bool) *RechargeCampaignRuleOverseaQuery {
	rcroq.ctx.Unique = &unique
	return rcroq
}

// Order specifies how the records should be ordered.
func (rcroq *RechargeCampaignRuleOverseaQuery) Order(o ...rechargecampaignruleoversea.OrderOption) *RechargeCampaignRuleOverseaQuery {
	rcroq.order = append(rcroq.order, o...)
	return rcroq
}

// First returns the first RechargeCampaignRuleOversea entity from the query.
// Returns a *NotFoundError when no RechargeCampaignRuleOversea was found.
func (rcroq *RechargeCampaignRuleOverseaQuery) First(ctx context.Context) (*RechargeCampaignRuleOversea, error) {
	nodes, err := rcroq.Limit(1).All(setContextOp(ctx, rcroq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{rechargecampaignruleoversea.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) FirstX(ctx context.Context) *RechargeCampaignRuleOversea {
	node, err := rcroq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RechargeCampaignRuleOversea ID from the query.
// Returns a *NotFoundError when no RechargeCampaignRuleOversea ID was found.
func (rcroq *RechargeCampaignRuleOverseaQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = rcroq.Limit(1).IDs(setContextOp(ctx, rcroq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{rechargecampaignruleoversea.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) FirstIDX(ctx context.Context) int64 {
	id, err := rcroq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RechargeCampaignRuleOversea entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RechargeCampaignRuleOversea entity is found.
// Returns a *NotFoundError when no RechargeCampaignRuleOversea entities are found.
func (rcroq *RechargeCampaignRuleOverseaQuery) Only(ctx context.Context) (*RechargeCampaignRuleOversea, error) {
	nodes, err := rcroq.Limit(2).All(setContextOp(ctx, rcroq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{rechargecampaignruleoversea.Label}
	default:
		return nil, &NotSingularError{rechargecampaignruleoversea.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) OnlyX(ctx context.Context) *RechargeCampaignRuleOversea {
	node, err := rcroq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RechargeCampaignRuleOversea ID in the query.
// Returns a *NotSingularError when more than one RechargeCampaignRuleOversea ID is found.
// Returns a *NotFoundError when no entities are found.
func (rcroq *RechargeCampaignRuleOverseaQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = rcroq.Limit(2).IDs(setContextOp(ctx, rcroq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{rechargecampaignruleoversea.Label}
	default:
		err = &NotSingularError{rechargecampaignruleoversea.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := rcroq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RechargeCampaignRuleOverseas.
func (rcroq *RechargeCampaignRuleOverseaQuery) All(ctx context.Context) ([]*RechargeCampaignRuleOversea, error) {
	ctx = setContextOp(ctx, rcroq.ctx, "All")
	if err := rcroq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RechargeCampaignRuleOversea, *RechargeCampaignRuleOverseaQuery]()
	return withInterceptors[[]*RechargeCampaignRuleOversea](ctx, rcroq, qr, rcroq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) AllX(ctx context.Context) []*RechargeCampaignRuleOversea {
	nodes, err := rcroq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RechargeCampaignRuleOversea IDs.
func (rcroq *RechargeCampaignRuleOverseaQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if rcroq.ctx.Unique == nil && rcroq.path != nil {
		rcroq.Unique(true)
	}
	ctx = setContextOp(ctx, rcroq.ctx, "IDs")
	if err = rcroq.Select(rechargecampaignruleoversea.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) IDsX(ctx context.Context) []int64 {
	ids, err := rcroq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rcroq *RechargeCampaignRuleOverseaQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rcroq.ctx, "Count")
	if err := rcroq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rcroq, querierCount[*RechargeCampaignRuleOverseaQuery](), rcroq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) CountX(ctx context.Context) int {
	count, err := rcroq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rcroq *RechargeCampaignRuleOverseaQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rcroq.ctx, "Exist")
	switch _, err := rcroq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rcroq *RechargeCampaignRuleOverseaQuery) ExistX(ctx context.Context) bool {
	exist, err := rcroq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RechargeCampaignRuleOverseaQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rcroq *RechargeCampaignRuleOverseaQuery) Clone() *RechargeCampaignRuleOverseaQuery {
	if rcroq == nil {
		return nil
	}
	return &RechargeCampaignRuleOverseaQuery{
		config:     rcroq.config,
		ctx:        rcroq.ctx.Clone(),
		order:      append([]rechargecampaignruleoversea.OrderOption{}, rcroq.order...),
		inters:     append([]Interceptor{}, rcroq.inters...),
		predicates: append([]predicate.RechargeCampaignRuleOversea{}, rcroq.predicates...),
		// clone intermediate query.
		sql:  rcroq.sql.Clone(),
		path: rcroq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RechargeCampaignRuleOversea.Query().
//		GroupBy(rechargecampaignruleoversea.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (rcroq *RechargeCampaignRuleOverseaQuery) GroupBy(field string, fields ...string) *RechargeCampaignRuleOverseaGroupBy {
	rcroq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RechargeCampaignRuleOverseaGroupBy{build: rcroq}
	grbuild.flds = &rcroq.ctx.Fields
	grbuild.label = rechargecampaignruleoversea.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//	}
//
//	client.RechargeCampaignRuleOversea.Query().
//		Select(rechargecampaignruleoversea.FieldCreatedBy).
//		Scan(ctx, &v)
func (rcroq *RechargeCampaignRuleOverseaQuery) Select(fields ...string) *RechargeCampaignRuleOverseaSelect {
	rcroq.ctx.Fields = append(rcroq.ctx.Fields, fields...)
	sbuild := &RechargeCampaignRuleOverseaSelect{RechargeCampaignRuleOverseaQuery: rcroq}
	sbuild.label = rechargecampaignruleoversea.Label
	sbuild.flds, sbuild.scan = &rcroq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RechargeCampaignRuleOverseaSelect configured with the given aggregations.
func (rcroq *RechargeCampaignRuleOverseaQuery) Aggregate(fns ...AggregateFunc) *RechargeCampaignRuleOverseaSelect {
	return rcroq.Select().Aggregate(fns...)
}

func (rcroq *RechargeCampaignRuleOverseaQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rcroq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rcroq); err != nil {
				return err
			}
		}
	}
	for _, f := range rcroq.ctx.Fields {
		if !rechargecampaignruleoversea.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if rcroq.path != nil {
		prev, err := rcroq.path(ctx)
		if err != nil {
			return err
		}
		rcroq.sql = prev
	}
	return nil
}

func (rcroq *RechargeCampaignRuleOverseaQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RechargeCampaignRuleOversea, error) {
	var (
		nodes = []*RechargeCampaignRuleOversea{}
		_spec = rcroq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RechargeCampaignRuleOversea).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RechargeCampaignRuleOversea{config: rcroq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(rcroq.modifiers) > 0 {
		_spec.Modifiers = rcroq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rcroq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rcroq *RechargeCampaignRuleOverseaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rcroq.querySpec()
	if len(rcroq.modifiers) > 0 {
		_spec.Modifiers = rcroq.modifiers
	}
	_spec.Node.Columns = rcroq.ctx.Fields
	if len(rcroq.ctx.Fields) > 0 {
		_spec.Unique = rcroq.ctx.Unique != nil && *rcroq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rcroq.driver, _spec)
}

func (rcroq *RechargeCampaignRuleOverseaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(rechargecampaignruleoversea.Table, rechargecampaignruleoversea.Columns, sqlgraph.NewFieldSpec(rechargecampaignruleoversea.FieldID, field.TypeInt64))
	_spec.From = rcroq.sql
	if unique := rcroq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rcroq.path != nil {
		_spec.Unique = true
	}
	if fields := rcroq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, rechargecampaignruleoversea.FieldID)
		for i := range fields {
			if fields[i] != rechargecampaignruleoversea.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rcroq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rcroq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rcroq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rcroq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rcroq *RechargeCampaignRuleOverseaQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rcroq.driver.Dialect())
	t1 := builder.Table(rechargecampaignruleoversea.Table)
	columns := rcroq.ctx.Fields
	if len(columns) == 0 {
		columns = rechargecampaignruleoversea.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rcroq.sql != nil {
		selector = rcroq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rcroq.ctx.Unique != nil && *rcroq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range rcroq.modifiers {
		m(selector)
	}
	for _, p := range rcroq.predicates {
		p(selector)
	}
	for _, p := range rcroq.order {
		p(selector)
	}
	if offset := rcroq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rcroq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rcroq *RechargeCampaignRuleOverseaQuery) Modify(modifiers ...func(s *sql.Selector)) *RechargeCampaignRuleOverseaSelect {
	rcroq.modifiers = append(rcroq.modifiers, modifiers...)
	return rcroq.Select()
}

// RechargeCampaignRuleOverseaGroupBy is the group-by builder for RechargeCampaignRuleOversea entities.
type RechargeCampaignRuleOverseaGroupBy struct {
	selector
	build *RechargeCampaignRuleOverseaQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rcrogb *RechargeCampaignRuleOverseaGroupBy) Aggregate(fns ...AggregateFunc) *RechargeCampaignRuleOverseaGroupBy {
	rcrogb.fns = append(rcrogb.fns, fns...)
	return rcrogb
}

// Scan applies the selector query and scans the result into the given value.
func (rcrogb *RechargeCampaignRuleOverseaGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcrogb.build.ctx, "GroupBy")
	if err := rcrogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RechargeCampaignRuleOverseaQuery, *RechargeCampaignRuleOverseaGroupBy](ctx, rcrogb.build, rcrogb, rcrogb.build.inters, v)
}

func (rcrogb *RechargeCampaignRuleOverseaGroupBy) sqlScan(ctx context.Context, root *RechargeCampaignRuleOverseaQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rcrogb.fns))
	for _, fn := range rcrogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rcrogb.flds)+len(rcrogb.fns))
		for _, f := range *rcrogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rcrogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcrogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RechargeCampaignRuleOverseaSelect is the builder for selecting fields of RechargeCampaignRuleOversea entities.
type RechargeCampaignRuleOverseaSelect struct {
	*RechargeCampaignRuleOverseaQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rcros *RechargeCampaignRuleOverseaSelect) Aggregate(fns ...AggregateFunc) *RechargeCampaignRuleOverseaSelect {
	rcros.fns = append(rcros.fns, fns...)
	return rcros
}

// Scan applies the selector query and scans the result into the given value.
func (rcros *RechargeCampaignRuleOverseaSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rcros.ctx, "Select")
	if err := rcros.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RechargeCampaignRuleOverseaQuery, *RechargeCampaignRuleOverseaSelect](ctx, rcros.RechargeCampaignRuleOverseaQuery, rcros, rcros.inters, v)
}

func (rcros *RechargeCampaignRuleOverseaSelect) sqlScan(ctx context.Context, root *RechargeCampaignRuleOverseaQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rcros.fns))
	for _, fn := range rcros.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rcros.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rcros.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (rcros *RechargeCampaignRuleOverseaSelect) Modify(modifiers ...func(s *sql.Selector)) *RechargeCampaignRuleOverseaSelect {
	rcros.modifiers = append(rcros.modifiers, modifiers...)
	return rcros
}

// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/missionloadbalance"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
)

// MissionLoadBalanceQuery is the builder for querying MissionLoadBalance entities.
type MissionLoadBalanceQuery struct {
	config
	ctx        *QueryContext
	order      []missionloadbalance.OrderOption
	inters     []Interceptor
	predicates []predicate.MissionLoadBalance
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MissionLoadBalanceQuery builder.
func (mlbq *MissionLoadBalanceQuery) Where(ps ...predicate.MissionLoadBalance) *MissionLoadBalanceQuery {
	mlbq.predicates = append(mlbq.predicates, ps...)
	return mlbq
}

// Limit the number of records to be returned by this query.
func (mlbq *MissionLoadBalanceQuery) Limit(limit int) *MissionLoadBalanceQuery {
	mlbq.ctx.Limit = &limit
	return mlbq
}

// Offset to start from.
func (mlbq *MissionLoadBalanceQuery) Offset(offset int) *MissionLoadBalanceQuery {
	mlbq.ctx.Offset = &offset
	return mlbq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mlbq *MissionLoadBalanceQuery) Unique(unique bool) *MissionLoadBalanceQuery {
	mlbq.ctx.Unique = &unique
	return mlbq
}

// Order specifies how the records should be ordered.
func (mlbq *MissionLoadBalanceQuery) Order(o ...missionloadbalance.OrderOption) *MissionLoadBalanceQuery {
	mlbq.order = append(mlbq.order, o...)
	return mlbq
}

// First returns the first MissionLoadBalance entity from the query.
// Returns a *NotFoundError when no MissionLoadBalance was found.
func (mlbq *MissionLoadBalanceQuery) First(ctx context.Context) (*MissionLoadBalance, error) {
	nodes, err := mlbq.Limit(1).All(setContextOp(ctx, mlbq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{missionloadbalance.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) FirstX(ctx context.Context) *MissionLoadBalance {
	node, err := mlbq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MissionLoadBalance ID from the query.
// Returns a *NotFoundError when no MissionLoadBalance ID was found.
func (mlbq *MissionLoadBalanceQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mlbq.Limit(1).IDs(setContextOp(ctx, mlbq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{missionloadbalance.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) FirstIDX(ctx context.Context) int64 {
	id, err := mlbq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MissionLoadBalance entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MissionLoadBalance entity is found.
// Returns a *NotFoundError when no MissionLoadBalance entities are found.
func (mlbq *MissionLoadBalanceQuery) Only(ctx context.Context) (*MissionLoadBalance, error) {
	nodes, err := mlbq.Limit(2).All(setContextOp(ctx, mlbq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{missionloadbalance.Label}
	default:
		return nil, &NotSingularError{missionloadbalance.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) OnlyX(ctx context.Context) *MissionLoadBalance {
	node, err := mlbq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MissionLoadBalance ID in the query.
// Returns a *NotSingularError when more than one MissionLoadBalance ID is found.
// Returns a *NotFoundError when no entities are found.
func (mlbq *MissionLoadBalanceQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mlbq.Limit(2).IDs(setContextOp(ctx, mlbq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{missionloadbalance.Label}
	default:
		err = &NotSingularError{missionloadbalance.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := mlbq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MissionLoadBalances.
func (mlbq *MissionLoadBalanceQuery) All(ctx context.Context) ([]*MissionLoadBalance, error) {
	ctx = setContextOp(ctx, mlbq.ctx, "All")
	if err := mlbq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MissionLoadBalance, *MissionLoadBalanceQuery]()
	return withInterceptors[[]*MissionLoadBalance](ctx, mlbq, qr, mlbq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) AllX(ctx context.Context) []*MissionLoadBalance {
	nodes, err := mlbq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MissionLoadBalance IDs.
func (mlbq *MissionLoadBalanceQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if mlbq.ctx.Unique == nil && mlbq.path != nil {
		mlbq.Unique(true)
	}
	ctx = setContextOp(ctx, mlbq.ctx, "IDs")
	if err = mlbq.Select(missionloadbalance.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) IDsX(ctx context.Context) []int64 {
	ids, err := mlbq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mlbq *MissionLoadBalanceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mlbq.ctx, "Count")
	if err := mlbq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mlbq, querierCount[*MissionLoadBalanceQuery](), mlbq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) CountX(ctx context.Context) int {
	count, err := mlbq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mlbq *MissionLoadBalanceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mlbq.ctx, "Exist")
	switch _, err := mlbq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mlbq *MissionLoadBalanceQuery) ExistX(ctx context.Context) bool {
	exist, err := mlbq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MissionLoadBalanceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mlbq *MissionLoadBalanceQuery) Clone() *MissionLoadBalanceQuery {
	if mlbq == nil {
		return nil
	}
	return &MissionLoadBalanceQuery{
		config:     mlbq.config,
		ctx:        mlbq.ctx.Clone(),
		order:      append([]missionloadbalance.OrderOption{}, mlbq.order...),
		inters:     append([]Interceptor{}, mlbq.inters...),
		predicates: append([]predicate.MissionLoadBalance{}, mlbq.predicates...),
		// clone intermediate query.
		sql:  mlbq.sql.Clone(),
		path: mlbq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MissionLoadBalance.Query().
//		GroupBy(missionloadbalance.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (mlbq *MissionLoadBalanceQuery) GroupBy(field string, fields ...string) *MissionLoadBalanceGroupBy {
	mlbq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MissionLoadBalanceGroupBy{build: mlbq}
	grbuild.flds = &mlbq.ctx.Fields
	grbuild.label = missionloadbalance.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//	}
//
//	client.MissionLoadBalance.Query().
//		Select(missionloadbalance.FieldCreatedBy).
//		Scan(ctx, &v)
func (mlbq *MissionLoadBalanceQuery) Select(fields ...string) *MissionLoadBalanceSelect {
	mlbq.ctx.Fields = append(mlbq.ctx.Fields, fields...)
	sbuild := &MissionLoadBalanceSelect{MissionLoadBalanceQuery: mlbq}
	sbuild.label = missionloadbalance.Label
	sbuild.flds, sbuild.scan = &mlbq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MissionLoadBalanceSelect configured with the given aggregations.
func (mlbq *MissionLoadBalanceQuery) Aggregate(fns ...AggregateFunc) *MissionLoadBalanceSelect {
	return mlbq.Select().Aggregate(fns...)
}

func (mlbq *MissionLoadBalanceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mlbq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mlbq); err != nil {
				return err
			}
		}
	}
	for _, f := range mlbq.ctx.Fields {
		if !missionloadbalance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if mlbq.path != nil {
		prev, err := mlbq.path(ctx)
		if err != nil {
			return err
		}
		mlbq.sql = prev
	}
	return nil
}

func (mlbq *MissionLoadBalanceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MissionLoadBalance, error) {
	var (
		nodes = []*MissionLoadBalance{}
		_spec = mlbq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MissionLoadBalance).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MissionLoadBalance{config: mlbq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(mlbq.modifiers) > 0 {
		_spec.Modifiers = mlbq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mlbq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mlbq *MissionLoadBalanceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mlbq.querySpec()
	if len(mlbq.modifiers) > 0 {
		_spec.Modifiers = mlbq.modifiers
	}
	_spec.Node.Columns = mlbq.ctx.Fields
	if len(mlbq.ctx.Fields) > 0 {
		_spec.Unique = mlbq.ctx.Unique != nil && *mlbq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mlbq.driver, _spec)
}

func (mlbq *MissionLoadBalanceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(missionloadbalance.Table, missionloadbalance.Columns, sqlgraph.NewFieldSpec(missionloadbalance.FieldID, field.TypeInt64))
	_spec.From = mlbq.sql
	if unique := mlbq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mlbq.path != nil {
		_spec.Unique = true
	}
	if fields := mlbq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, missionloadbalance.FieldID)
		for i := range fields {
			if fields[i] != missionloadbalance.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mlbq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mlbq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mlbq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mlbq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mlbq *MissionLoadBalanceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mlbq.driver.Dialect())
	t1 := builder.Table(missionloadbalance.Table)
	columns := mlbq.ctx.Fields
	if len(columns) == 0 {
		columns = missionloadbalance.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mlbq.sql != nil {
		selector = mlbq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mlbq.ctx.Unique != nil && *mlbq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mlbq.modifiers {
		m(selector)
	}
	for _, p := range mlbq.predicates {
		p(selector)
	}
	for _, p := range mlbq.order {
		p(selector)
	}
	if offset := mlbq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mlbq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mlbq *MissionLoadBalanceQuery) Modify(modifiers ...func(s *sql.Selector)) *MissionLoadBalanceSelect {
	mlbq.modifiers = append(mlbq.modifiers, modifiers...)
	return mlbq.Select()
}

// MissionLoadBalanceGroupBy is the group-by builder for MissionLoadBalance entities.
type MissionLoadBalanceGroupBy struct {
	selector
	build *MissionLoadBalanceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mlbgb *MissionLoadBalanceGroupBy) Aggregate(fns ...AggregateFunc) *MissionLoadBalanceGroupBy {
	mlbgb.fns = append(mlbgb.fns, fns...)
	return mlbgb
}

// Scan applies the selector query and scans the result into the given value.
func (mlbgb *MissionLoadBalanceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mlbgb.build.ctx, "GroupBy")
	if err := mlbgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MissionLoadBalanceQuery, *MissionLoadBalanceGroupBy](ctx, mlbgb.build, mlbgb, mlbgb.build.inters, v)
}

func (mlbgb *MissionLoadBalanceGroupBy) sqlScan(ctx context.Context, root *MissionLoadBalanceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mlbgb.fns))
	for _, fn := range mlbgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mlbgb.flds)+len(mlbgb.fns))
		for _, f := range *mlbgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mlbgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mlbgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MissionLoadBalanceSelect is the builder for selecting fields of MissionLoadBalance entities.
type MissionLoadBalanceSelect struct {
	*MissionLoadBalanceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mlbs *MissionLoadBalanceSelect) Aggregate(fns ...AggregateFunc) *MissionLoadBalanceSelect {
	mlbs.fns = append(mlbs.fns, fns...)
	return mlbs
}

// Scan applies the selector query and scans the result into the given value.
func (mlbs *MissionLoadBalanceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mlbs.ctx, "Select")
	if err := mlbs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MissionLoadBalanceQuery, *MissionLoadBalanceSelect](ctx, mlbs.MissionLoadBalanceQuery, mlbs, mlbs.inters, v)
}

func (mlbs *MissionLoadBalanceSelect) sqlScan(ctx context.Context, root *MissionLoadBalanceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mlbs.fns))
	for _, fn := range mlbs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mlbs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mlbs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mlbs *MissionLoadBalanceSelect) Modify(modifiers ...func(s *sql.Selector)) *MissionLoadBalanceSelect {
	mlbs.modifiers = append(mlbs.modifiers, modifiers...)
	return mlbs
}

// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"cephalon-ent/pkg/cep_ent/device"
	"cephalon-ent/pkg/cep_ent/devicegpumission"
	"cephalon-ent/pkg/cep_ent/gpu"
	"cephalon-ent/pkg/cep_ent/missionkind"
	"cephalon-ent/pkg/cep_ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DeviceGpuMissionQuery is the builder for querying DeviceGpuMission entities.
type DeviceGpuMissionQuery struct {
	config
	ctx             *QueryContext
	order           []devicegpumission.OrderOption
	inters          []Interceptor
	predicates      []predicate.DeviceGpuMission
	withDevice      *DeviceQuery
	withMissionKind *MissionKindQuery
	withGpu         *GpuQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeviceGpuMissionQuery builder.
func (dgmq *DeviceGpuMissionQuery) Where(ps ...predicate.DeviceGpuMission) *DeviceGpuMissionQuery {
	dgmq.predicates = append(dgmq.predicates, ps...)
	return dgmq
}

// Limit the number of records to be returned by this query.
func (dgmq *DeviceGpuMissionQuery) Limit(limit int) *DeviceGpuMissionQuery {
	dgmq.ctx.Limit = &limit
	return dgmq
}

// Offset to start from.
func (dgmq *DeviceGpuMissionQuery) Offset(offset int) *DeviceGpuMissionQuery {
	dgmq.ctx.Offset = &offset
	return dgmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dgmq *DeviceGpuMissionQuery) Unique(unique bool) *DeviceGpuMissionQuery {
	dgmq.ctx.Unique = &unique
	return dgmq
}

// Order specifies how the records should be ordered.
func (dgmq *DeviceGpuMissionQuery) Order(o ...devicegpumission.OrderOption) *DeviceGpuMissionQuery {
	dgmq.order = append(dgmq.order, o...)
	return dgmq
}

// QueryDevice chains the current query on the "device" edge.
func (dgmq *DeviceGpuMissionQuery) QueryDevice() *DeviceQuery {
	query := (&DeviceClient{config: dgmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dgmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dgmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(devicegpumission.Table, devicegpumission.FieldID, selector),
			sqlgraph.To(device.Table, device.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, devicegpumission.DeviceTable, devicegpumission.DeviceColumn),
		)
		fromU = sqlgraph.SetNeighbors(dgmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMissionKind chains the current query on the "mission_kind" edge.
func (dgmq *DeviceGpuMissionQuery) QueryMissionKind() *MissionKindQuery {
	query := (&MissionKindClient{config: dgmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dgmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dgmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(devicegpumission.Table, devicegpumission.FieldID, selector),
			sqlgraph.To(missionkind.Table, missionkind.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, devicegpumission.MissionKindTable, devicegpumission.MissionKindColumn),
		)
		fromU = sqlgraph.SetNeighbors(dgmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGpu chains the current query on the "gpu" edge.
func (dgmq *DeviceGpuMissionQuery) QueryGpu() *GpuQuery {
	query := (&GpuClient{config: dgmq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dgmq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dgmq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(devicegpumission.Table, devicegpumission.FieldID, selector),
			sqlgraph.To(gpu.Table, gpu.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, devicegpumission.GpuTable, devicegpumission.GpuColumn),
		)
		fromU = sqlgraph.SetNeighbors(dgmq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DeviceGpuMission entity from the query.
// Returns a *NotFoundError when no DeviceGpuMission was found.
func (dgmq *DeviceGpuMissionQuery) First(ctx context.Context) (*DeviceGpuMission, error) {
	nodes, err := dgmq.Limit(1).All(setContextOp(ctx, dgmq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{devicegpumission.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) FirstX(ctx context.Context) *DeviceGpuMission {
	node, err := dgmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeviceGpuMission ID from the query.
// Returns a *NotFoundError when no DeviceGpuMission ID was found.
func (dgmq *DeviceGpuMissionQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dgmq.Limit(1).IDs(setContextOp(ctx, dgmq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{devicegpumission.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) FirstIDX(ctx context.Context) int64 {
	id, err := dgmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeviceGpuMission entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeviceGpuMission entity is found.
// Returns a *NotFoundError when no DeviceGpuMission entities are found.
func (dgmq *DeviceGpuMissionQuery) Only(ctx context.Context) (*DeviceGpuMission, error) {
	nodes, err := dgmq.Limit(2).All(setContextOp(ctx, dgmq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{devicegpumission.Label}
	default:
		return nil, &NotSingularError{devicegpumission.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) OnlyX(ctx context.Context) *DeviceGpuMission {
	node, err := dgmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeviceGpuMission ID in the query.
// Returns a *NotSingularError when more than one DeviceGpuMission ID is found.
// Returns a *NotFoundError when no entities are found.
func (dgmq *DeviceGpuMissionQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = dgmq.Limit(2).IDs(setContextOp(ctx, dgmq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{devicegpumission.Label}
	default:
		err = &NotSingularError{devicegpumission.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := dgmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeviceGpuMissions.
func (dgmq *DeviceGpuMissionQuery) All(ctx context.Context) ([]*DeviceGpuMission, error) {
	ctx = setContextOp(ctx, dgmq.ctx, "All")
	if err := dgmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeviceGpuMission, *DeviceGpuMissionQuery]()
	return withInterceptors[[]*DeviceGpuMission](ctx, dgmq, qr, dgmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) AllX(ctx context.Context) []*DeviceGpuMission {
	nodes, err := dgmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeviceGpuMission IDs.
func (dgmq *DeviceGpuMissionQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if dgmq.ctx.Unique == nil && dgmq.path != nil {
		dgmq.Unique(true)
	}
	ctx = setContextOp(ctx, dgmq.ctx, "IDs")
	if err = dgmq.Select(devicegpumission.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) IDsX(ctx context.Context) []int64 {
	ids, err := dgmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dgmq *DeviceGpuMissionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dgmq.ctx, "Count")
	if err := dgmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dgmq, querierCount[*DeviceGpuMissionQuery](), dgmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) CountX(ctx context.Context) int {
	count, err := dgmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dgmq *DeviceGpuMissionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dgmq.ctx, "Exist")
	switch _, err := dgmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dgmq *DeviceGpuMissionQuery) ExistX(ctx context.Context) bool {
	exist, err := dgmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeviceGpuMissionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dgmq *DeviceGpuMissionQuery) Clone() *DeviceGpuMissionQuery {
	if dgmq == nil {
		return nil
	}
	return &DeviceGpuMissionQuery{
		config:          dgmq.config,
		ctx:             dgmq.ctx.Clone(),
		order:           append([]devicegpumission.OrderOption{}, dgmq.order...),
		inters:          append([]Interceptor{}, dgmq.inters...),
		predicates:      append([]predicate.DeviceGpuMission{}, dgmq.predicates...),
		withDevice:      dgmq.withDevice.Clone(),
		withMissionKind: dgmq.withMissionKind.Clone(),
		withGpu:         dgmq.withGpu.Clone(),
		// clone intermediate query.
		sql:  dgmq.sql.Clone(),
		path: dgmq.path,
	}
}

// WithDevice tells the query-builder to eager-load the nodes that are connected to
// the "device" edge. The optional arguments are used to configure the query builder of the edge.
func (dgmq *DeviceGpuMissionQuery) WithDevice(opts ...func(*DeviceQuery)) *DeviceGpuMissionQuery {
	query := (&DeviceClient{config: dgmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dgmq.withDevice = query
	return dgmq
}

// WithMissionKind tells the query-builder to eager-load the nodes that are connected to
// the "mission_kind" edge. The optional arguments are used to configure the query builder of the edge.
func (dgmq *DeviceGpuMissionQuery) WithMissionKind(opts ...func(*MissionKindQuery)) *DeviceGpuMissionQuery {
	query := (&MissionKindClient{config: dgmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dgmq.withMissionKind = query
	return dgmq
}

// WithGpu tells the query-builder to eager-load the nodes that are connected to
// the "gpu" edge. The optional arguments are used to configure the query builder of the edge.
func (dgmq *DeviceGpuMissionQuery) WithGpu(opts ...func(*GpuQuery)) *DeviceGpuMissionQuery {
	query := (&GpuClient{config: dgmq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dgmq.withGpu = query
	return dgmq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DeviceGpuMission.Query().
//		GroupBy(devicegpumission.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (dgmq *DeviceGpuMissionQuery) GroupBy(field string, fields ...string) *DeviceGpuMissionGroupBy {
	dgmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeviceGpuMissionGroupBy{build: dgmq}
	grbuild.flds = &dgmq.ctx.Fields
	grbuild.label = devicegpumission.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//	}
//
//	client.DeviceGpuMission.Query().
//		Select(devicegpumission.FieldCreatedBy).
//		Scan(ctx, &v)
func (dgmq *DeviceGpuMissionQuery) Select(fields ...string) *DeviceGpuMissionSelect {
	dgmq.ctx.Fields = append(dgmq.ctx.Fields, fields...)
	sbuild := &DeviceGpuMissionSelect{DeviceGpuMissionQuery: dgmq}
	sbuild.label = devicegpumission.Label
	sbuild.flds, sbuild.scan = &dgmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeviceGpuMissionSelect configured with the given aggregations.
func (dgmq *DeviceGpuMissionQuery) Aggregate(fns ...AggregateFunc) *DeviceGpuMissionSelect {
	return dgmq.Select().Aggregate(fns...)
}

func (dgmq *DeviceGpuMissionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dgmq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dgmq); err != nil {
				return err
			}
		}
	}
	for _, f := range dgmq.ctx.Fields {
		if !devicegpumission.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if dgmq.path != nil {
		prev, err := dgmq.path(ctx)
		if err != nil {
			return err
		}
		dgmq.sql = prev
	}
	return nil
}

func (dgmq *DeviceGpuMissionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeviceGpuMission, error) {
	var (
		nodes       = []*DeviceGpuMission{}
		_spec       = dgmq.querySpec()
		loadedTypes = [3]bool{
			dgmq.withDevice != nil,
			dgmq.withMissionKind != nil,
			dgmq.withGpu != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeviceGpuMission).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeviceGpuMission{config: dgmq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dgmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dgmq.withDevice; query != nil {
		if err := dgmq.loadDevice(ctx, query, nodes, nil,
			func(n *DeviceGpuMission, e *Device) { n.Edges.Device = e }); err != nil {
			return nil, err
		}
	}
	if query := dgmq.withMissionKind; query != nil {
		if err := dgmq.loadMissionKind(ctx, query, nodes, nil,
			func(n *DeviceGpuMission, e *MissionKind) { n.Edges.MissionKind = e }); err != nil {
			return nil, err
		}
	}
	if query := dgmq.withGpu; query != nil {
		if err := dgmq.loadGpu(ctx, query, nodes, nil,
			func(n *DeviceGpuMission, e *Gpu) { n.Edges.Gpu = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dgmq *DeviceGpuMissionQuery) loadDevice(ctx context.Context, query *DeviceQuery, nodes []*DeviceGpuMission, init func(*DeviceGpuMission), assign func(*DeviceGpuMission, *Device)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*DeviceGpuMission)
	for i := range nodes {
		fk := nodes[i].DeviceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(device.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "device_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dgmq *DeviceGpuMissionQuery) loadMissionKind(ctx context.Context, query *MissionKindQuery, nodes []*DeviceGpuMission, init func(*DeviceGpuMission), assign func(*DeviceGpuMission, *MissionKind)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*DeviceGpuMission)
	for i := range nodes {
		fk := nodes[i].MissionKindID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(missionkind.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "mission_kind_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (dgmq *DeviceGpuMissionQuery) loadGpu(ctx context.Context, query *GpuQuery, nodes []*DeviceGpuMission, init func(*DeviceGpuMission), assign func(*DeviceGpuMission, *Gpu)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*DeviceGpuMission)
	for i := range nodes {
		fk := nodes[i].GpuID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(gpu.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "gpu_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dgmq *DeviceGpuMissionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dgmq.querySpec()
	_spec.Node.Columns = dgmq.ctx.Fields
	if len(dgmq.ctx.Fields) > 0 {
		_spec.Unique = dgmq.ctx.Unique != nil && *dgmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dgmq.driver, _spec)
}

func (dgmq *DeviceGpuMissionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(devicegpumission.Table, devicegpumission.Columns, sqlgraph.NewFieldSpec(devicegpumission.FieldID, field.TypeInt64))
	_spec.From = dgmq.sql
	if unique := dgmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dgmq.path != nil {
		_spec.Unique = true
	}
	if fields := dgmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, devicegpumission.FieldID)
		for i := range fields {
			if fields[i] != devicegpumission.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dgmq.withDevice != nil {
			_spec.Node.AddColumnOnce(devicegpumission.FieldDeviceID)
		}
		if dgmq.withMissionKind != nil {
			_spec.Node.AddColumnOnce(devicegpumission.FieldMissionKindID)
		}
		if dgmq.withGpu != nil {
			_spec.Node.AddColumnOnce(devicegpumission.FieldGpuID)
		}
	}
	if ps := dgmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dgmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dgmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dgmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dgmq *DeviceGpuMissionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dgmq.driver.Dialect())
	t1 := builder.Table(devicegpumission.Table)
	columns := dgmq.ctx.Fields
	if len(columns) == 0 {
		columns = devicegpumission.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dgmq.sql != nil {
		selector = dgmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dgmq.ctx.Unique != nil && *dgmq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dgmq.predicates {
		p(selector)
	}
	for _, p := range dgmq.order {
		p(selector)
	}
	if offset := dgmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dgmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DeviceGpuMissionGroupBy is the group-by builder for DeviceGpuMission entities.
type DeviceGpuMissionGroupBy struct {
	selector
	build *DeviceGpuMissionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dgmgb *DeviceGpuMissionGroupBy) Aggregate(fns ...AggregateFunc) *DeviceGpuMissionGroupBy {
	dgmgb.fns = append(dgmgb.fns, fns...)
	return dgmgb
}

// Scan applies the selector query and scans the result into the given value.
func (dgmgb *DeviceGpuMissionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dgmgb.build.ctx, "GroupBy")
	if err := dgmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeviceGpuMissionQuery, *DeviceGpuMissionGroupBy](ctx, dgmgb.build, dgmgb, dgmgb.build.inters, v)
}

func (dgmgb *DeviceGpuMissionGroupBy) sqlScan(ctx context.Context, root *DeviceGpuMissionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dgmgb.fns))
	for _, fn := range dgmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dgmgb.flds)+len(dgmgb.fns))
		for _, f := range *dgmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dgmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dgmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeviceGpuMissionSelect is the builder for selecting fields of DeviceGpuMission entities.
type DeviceGpuMissionSelect struct {
	*DeviceGpuMissionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dgms *DeviceGpuMissionSelect) Aggregate(fns ...AggregateFunc) *DeviceGpuMissionSelect {
	dgms.fns = append(dgms.fns, fns...)
	return dgms
}

// Scan applies the selector query and scans the result into the given value.
func (dgms *DeviceGpuMissionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dgms.ctx, "Select")
	if err := dgms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeviceGpuMissionQuery, *DeviceGpuMissionSelect](ctx, dgms.DeviceGpuMissionQuery, dgms, dgms.inters, v)
}

func (dgms *DeviceGpuMissionSelect) sqlScan(ctx context.Context, root *DeviceGpuMissionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dgms.fns))
	for _, fn := range dgms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dgms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dgms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

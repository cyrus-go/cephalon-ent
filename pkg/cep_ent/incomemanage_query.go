// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/incomemanage"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/symbol"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/user"
)

// IncomeManageQuery is the builder for querying IncomeManage entities.
type IncomeManageQuery struct {
	config
	ctx             *QueryContext
	order           []incomemanage.OrderOption
	inters          []Interceptor
	predicates      []predicate.IncomeManage
	withUser        *UserQuery
	withApproveUser *UserQuery
	withSymbol      *SymbolQuery
	modifiers       []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the IncomeManageQuery builder.
func (imq *IncomeManageQuery) Where(ps ...predicate.IncomeManage) *IncomeManageQuery {
	imq.predicates = append(imq.predicates, ps...)
	return imq
}

// Limit the number of records to be returned by this query.
func (imq *IncomeManageQuery) Limit(limit int) *IncomeManageQuery {
	imq.ctx.Limit = &limit
	return imq
}

// Offset to start from.
func (imq *IncomeManageQuery) Offset(offset int) *IncomeManageQuery {
	imq.ctx.Offset = &offset
	return imq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (imq *IncomeManageQuery) Unique(unique bool) *IncomeManageQuery {
	imq.ctx.Unique = &unique
	return imq
}

// Order specifies how the records should be ordered.
func (imq *IncomeManageQuery) Order(o ...incomemanage.OrderOption) *IncomeManageQuery {
	imq.order = append(imq.order, o...)
	return imq
}

// QueryUser chains the current query on the "user" edge.
func (imq *IncomeManageQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: imq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := imq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := imq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(incomemanage.Table, incomemanage.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, incomemanage.UserTable, incomemanage.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(imq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryApproveUser chains the current query on the "approve_user" edge.
func (imq *IncomeManageQuery) QueryApproveUser() *UserQuery {
	query := (&UserClient{config: imq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := imq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := imq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(incomemanage.Table, incomemanage.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, incomemanage.ApproveUserTable, incomemanage.ApproveUserColumn),
		)
		fromU = sqlgraph.SetNeighbors(imq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySymbol chains the current query on the "symbol" edge.
func (imq *IncomeManageQuery) QuerySymbol() *SymbolQuery {
	query := (&SymbolClient{config: imq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := imq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := imq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(incomemanage.Table, incomemanage.FieldID, selector),
			sqlgraph.To(symbol.Table, symbol.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, incomemanage.SymbolTable, incomemanage.SymbolColumn),
		)
		fromU = sqlgraph.SetNeighbors(imq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first IncomeManage entity from the query.
// Returns a *NotFoundError when no IncomeManage was found.
func (imq *IncomeManageQuery) First(ctx context.Context) (*IncomeManage, error) {
	nodes, err := imq.Limit(1).All(setContextOp(ctx, imq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{incomemanage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (imq *IncomeManageQuery) FirstX(ctx context.Context) *IncomeManage {
	node, err := imq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first IncomeManage ID from the query.
// Returns a *NotFoundError when no IncomeManage ID was found.
func (imq *IncomeManageQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = imq.Limit(1).IDs(setContextOp(ctx, imq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{incomemanage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (imq *IncomeManageQuery) FirstIDX(ctx context.Context) int64 {
	id, err := imq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single IncomeManage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one IncomeManage entity is found.
// Returns a *NotFoundError when no IncomeManage entities are found.
func (imq *IncomeManageQuery) Only(ctx context.Context) (*IncomeManage, error) {
	nodes, err := imq.Limit(2).All(setContextOp(ctx, imq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{incomemanage.Label}
	default:
		return nil, &NotSingularError{incomemanage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (imq *IncomeManageQuery) OnlyX(ctx context.Context) *IncomeManage {
	node, err := imq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only IncomeManage ID in the query.
// Returns a *NotSingularError when more than one IncomeManage ID is found.
// Returns a *NotFoundError when no entities are found.
func (imq *IncomeManageQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = imq.Limit(2).IDs(setContextOp(ctx, imq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{incomemanage.Label}
	default:
		err = &NotSingularError{incomemanage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (imq *IncomeManageQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := imq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of IncomeManages.
func (imq *IncomeManageQuery) All(ctx context.Context) ([]*IncomeManage, error) {
	ctx = setContextOp(ctx, imq.ctx, "All")
	if err := imq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*IncomeManage, *IncomeManageQuery]()
	return withInterceptors[[]*IncomeManage](ctx, imq, qr, imq.inters)
}

// AllX is like All, but panics if an error occurs.
func (imq *IncomeManageQuery) AllX(ctx context.Context) []*IncomeManage {
	nodes, err := imq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of IncomeManage IDs.
func (imq *IncomeManageQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if imq.ctx.Unique == nil && imq.path != nil {
		imq.Unique(true)
	}
	ctx = setContextOp(ctx, imq.ctx, "IDs")
	if err = imq.Select(incomemanage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (imq *IncomeManageQuery) IDsX(ctx context.Context) []int64 {
	ids, err := imq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (imq *IncomeManageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, imq.ctx, "Count")
	if err := imq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, imq, querierCount[*IncomeManageQuery](), imq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (imq *IncomeManageQuery) CountX(ctx context.Context) int {
	count, err := imq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (imq *IncomeManageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, imq.ctx, "Exist")
	switch _, err := imq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (imq *IncomeManageQuery) ExistX(ctx context.Context) bool {
	exist, err := imq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the IncomeManageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (imq *IncomeManageQuery) Clone() *IncomeManageQuery {
	if imq == nil {
		return nil
	}
	return &IncomeManageQuery{
		config:          imq.config,
		ctx:             imq.ctx.Clone(),
		order:           append([]incomemanage.OrderOption{}, imq.order...),
		inters:          append([]Interceptor{}, imq.inters...),
		predicates:      append([]predicate.IncomeManage{}, imq.predicates...),
		withUser:        imq.withUser.Clone(),
		withApproveUser: imq.withApproveUser.Clone(),
		withSymbol:      imq.withSymbol.Clone(),
		// clone intermediate query.
		sql:  imq.sql.Clone(),
		path: imq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (imq *IncomeManageQuery) WithUser(opts ...func(*UserQuery)) *IncomeManageQuery {
	query := (&UserClient{config: imq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	imq.withUser = query
	return imq
}

// WithApproveUser tells the query-builder to eager-load the nodes that are connected to
// the "approve_user" edge. The optional arguments are used to configure the query builder of the edge.
func (imq *IncomeManageQuery) WithApproveUser(opts ...func(*UserQuery)) *IncomeManageQuery {
	query := (&UserClient{config: imq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	imq.withApproveUser = query
	return imq
}

// WithSymbol tells the query-builder to eager-load the nodes that are connected to
// the "symbol" edge. The optional arguments are used to configure the query builder of the edge.
func (imq *IncomeManageQuery) WithSymbol(opts ...func(*SymbolQuery)) *IncomeManageQuery {
	query := (&SymbolClient{config: imq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	imq.withSymbol = query
	return imq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.IncomeManage.Query().
//		GroupBy(incomemanage.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (imq *IncomeManageQuery) GroupBy(field string, fields ...string) *IncomeManageGroupBy {
	imq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &IncomeManageGroupBy{build: imq}
	grbuild.flds = &imq.ctx.Fields
	grbuild.label = incomemanage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by,string"`
//	}
//
//	client.IncomeManage.Query().
//		Select(incomemanage.FieldCreatedBy).
//		Scan(ctx, &v)
func (imq *IncomeManageQuery) Select(fields ...string) *IncomeManageSelect {
	imq.ctx.Fields = append(imq.ctx.Fields, fields...)
	sbuild := &IncomeManageSelect{IncomeManageQuery: imq}
	sbuild.label = incomemanage.Label
	sbuild.flds, sbuild.scan = &imq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a IncomeManageSelect configured with the given aggregations.
func (imq *IncomeManageQuery) Aggregate(fns ...AggregateFunc) *IncomeManageSelect {
	return imq.Select().Aggregate(fns...)
}

func (imq *IncomeManageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range imq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, imq); err != nil {
				return err
			}
		}
	}
	for _, f := range imq.ctx.Fields {
		if !incomemanage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if imq.path != nil {
		prev, err := imq.path(ctx)
		if err != nil {
			return err
		}
		imq.sql = prev
	}
	return nil
}

func (imq *IncomeManageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*IncomeManage, error) {
	var (
		nodes       = []*IncomeManage{}
		_spec       = imq.querySpec()
		loadedTypes = [3]bool{
			imq.withUser != nil,
			imq.withApproveUser != nil,
			imq.withSymbol != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*IncomeManage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &IncomeManage{config: imq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(imq.modifiers) > 0 {
		_spec.Modifiers = imq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, imq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := imq.withUser; query != nil {
		if err := imq.loadUser(ctx, query, nodes, nil,
			func(n *IncomeManage, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := imq.withApproveUser; query != nil {
		if err := imq.loadApproveUser(ctx, query, nodes, nil,
			func(n *IncomeManage, e *User) { n.Edges.ApproveUser = e }); err != nil {
			return nil, err
		}
	}
	if query := imq.withSymbol; query != nil {
		if err := imq.loadSymbol(ctx, query, nodes, nil,
			func(n *IncomeManage, e *Symbol) { n.Edges.Symbol = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (imq *IncomeManageQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*IncomeManage, init func(*IncomeManage), assign func(*IncomeManage, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*IncomeManage)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (imq *IncomeManageQuery) loadApproveUser(ctx context.Context, query *UserQuery, nodes []*IncomeManage, init func(*IncomeManage), assign func(*IncomeManage, *User)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*IncomeManage)
	for i := range nodes {
		fk := nodes[i].ApproveUserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "approve_user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (imq *IncomeManageQuery) loadSymbol(ctx context.Context, query *SymbolQuery, nodes []*IncomeManage, init func(*IncomeManage), assign func(*IncomeManage, *Symbol)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*IncomeManage)
	for i := range nodes {
		fk := nodes[i].SymbolID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(symbol.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "symbol_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (imq *IncomeManageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := imq.querySpec()
	if len(imq.modifiers) > 0 {
		_spec.Modifiers = imq.modifiers
	}
	_spec.Node.Columns = imq.ctx.Fields
	if len(imq.ctx.Fields) > 0 {
		_spec.Unique = imq.ctx.Unique != nil && *imq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, imq.driver, _spec)
}

func (imq *IncomeManageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(incomemanage.Table, incomemanage.Columns, sqlgraph.NewFieldSpec(incomemanage.FieldID, field.TypeInt64))
	_spec.From = imq.sql
	if unique := imq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if imq.path != nil {
		_spec.Unique = true
	}
	if fields := imq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, incomemanage.FieldID)
		for i := range fields {
			if fields[i] != incomemanage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if imq.withUser != nil {
			_spec.Node.AddColumnOnce(incomemanage.FieldUserID)
		}
		if imq.withApproveUser != nil {
			_spec.Node.AddColumnOnce(incomemanage.FieldApproveUserID)
		}
		if imq.withSymbol != nil {
			_spec.Node.AddColumnOnce(incomemanage.FieldSymbolID)
		}
	}
	if ps := imq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := imq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := imq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := imq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (imq *IncomeManageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(imq.driver.Dialect())
	t1 := builder.Table(incomemanage.Table)
	columns := imq.ctx.Fields
	if len(columns) == 0 {
		columns = incomemanage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if imq.sql != nil {
		selector = imq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if imq.ctx.Unique != nil && *imq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range imq.modifiers {
		m(selector)
	}
	for _, p := range imq.predicates {
		p(selector)
	}
	for _, p := range imq.order {
		p(selector)
	}
	if offset := imq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := imq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (imq *IncomeManageQuery) Modify(modifiers ...func(s *sql.Selector)) *IncomeManageSelect {
	imq.modifiers = append(imq.modifiers, modifiers...)
	return imq.Select()
}

// IncomeManageGroupBy is the group-by builder for IncomeManage entities.
type IncomeManageGroupBy struct {
	selector
	build *IncomeManageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (imgb *IncomeManageGroupBy) Aggregate(fns ...AggregateFunc) *IncomeManageGroupBy {
	imgb.fns = append(imgb.fns, fns...)
	return imgb
}

// Scan applies the selector query and scans the result into the given value.
func (imgb *IncomeManageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, imgb.build.ctx, "GroupBy")
	if err := imgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IncomeManageQuery, *IncomeManageGroupBy](ctx, imgb.build, imgb, imgb.build.inters, v)
}

func (imgb *IncomeManageGroupBy) sqlScan(ctx context.Context, root *IncomeManageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(imgb.fns))
	for _, fn := range imgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*imgb.flds)+len(imgb.fns))
		for _, f := range *imgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*imgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := imgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// IncomeManageSelect is the builder for selecting fields of IncomeManage entities.
type IncomeManageSelect struct {
	*IncomeManageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ims *IncomeManageSelect) Aggregate(fns ...AggregateFunc) *IncomeManageSelect {
	ims.fns = append(ims.fns, fns...)
	return ims
}

// Scan applies the selector query and scans the result into the given value.
func (ims *IncomeManageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ims.ctx, "Select")
	if err := ims.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*IncomeManageQuery, *IncomeManageSelect](ctx, ims.IncomeManageQuery, ims, ims.inters, v)
}

func (ims *IncomeManageSelect) sqlScan(ctx context.Context, root *IncomeManageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ims.fns))
	for _, fn := range ims.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ims.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ims.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ims *IncomeManageSelect) Modify(modifiers ...func(s *sql.Selector)) *IncomeManageSelect {
	ims.modifiers = append(ims.modifiers, modifiers...)
	return ims
}

// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/device"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/devicestate"
)

// 设备状态信息 目前只有延迟信息
type DeviceState struct {
	config `json:"-"`
	// ID of the ent.
	// 19 位雪花 ID
	ID int64 `json:"id,string"`
	// 创建者 ID
	CreatedBy int64 `json:"created_by,string"`
	// 更新者 ID
	UpdatedBy int64 `json:"updated_by,string"`
	// 创建时刻，带时区
	CreatedAt time.Time `json:"created_at"`
	// 更新时刻，带时区
	UpdatedAt time.Time `json:"updated_at"`
	// 软删除时刻，带时区
	DeletedAt time.Time `json:"deleted_at"`
	// 外键设备 id
	DeviceID int64 `json:"device_id,string"`
	// 延迟(单位:ms)
	Delay float64 `json:"delay"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceStateQuery when eager-loading is set.
	Edges        DeviceStateEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeviceStateEdges holds the relations/edges for other nodes in the graph.
type DeviceStateEdges struct {
	// Device holds the value of the device edge.
	Device *Device `json:"device,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DeviceOrErr returns the Device value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DeviceStateEdges) DeviceOrErr() (*Device, error) {
	if e.loadedTypes[0] {
		if e.Device == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: device.Label}
		}
		return e.Device, nil
	}
	return nil, &NotLoadedError{edge: "device"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceState) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case devicestate.FieldDelay:
			values[i] = new(sql.NullFloat64)
		case devicestate.FieldID, devicestate.FieldCreatedBy, devicestate.FieldUpdatedBy, devicestate.FieldDeviceID:
			values[i] = new(sql.NullInt64)
		case devicestate.FieldCreatedAt, devicestate.FieldUpdatedAt, devicestate.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceState fields.
func (ds *DeviceState) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case devicestate.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ds.ID = int64(value.Int64)
		case devicestate.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				ds.CreatedBy = value.Int64
			}
		case devicestate.FieldUpdatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_by", values[i])
			} else if value.Valid {
				ds.UpdatedBy = value.Int64
			}
		case devicestate.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ds.CreatedAt = value.Time
			}
		case devicestate.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ds.UpdatedAt = value.Time
			}
		case devicestate.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				ds.DeletedAt = value.Time
			}
		case devicestate.FieldDeviceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value.Valid {
				ds.DeviceID = value.Int64
			}
		case devicestate.FieldDelay:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field delay", values[i])
			} else if value.Valid {
				ds.Delay = value.Float64
			}
		default:
			ds.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceState.
// This includes values selected through modifiers, order, etc.
func (ds *DeviceState) Value(name string) (ent.Value, error) {
	return ds.selectValues.Get(name)
}

// QueryDevice queries the "device" edge of the DeviceState entity.
func (ds *DeviceState) QueryDevice() *DeviceQuery {
	return NewDeviceStateClient(ds.config).QueryDevice(ds)
}

// Update returns a builder for updating this DeviceState.
// Note that you need to call DeviceState.Unwrap() before calling this method if this DeviceState
// was returned from a transaction, and the transaction was committed or rolled back.
func (ds *DeviceState) Update() *DeviceStateUpdateOne {
	return NewDeviceStateClient(ds.config).UpdateOne(ds)
}

// Unwrap unwraps the DeviceState entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ds *DeviceState) Unwrap() *DeviceState {
	_tx, ok := ds.config.driver.(*txDriver)
	if !ok {
		panic("cep_ent: DeviceState is not a transactional entity")
	}
	ds.config.driver = _tx.drv
	return ds
}

// String implements the fmt.Stringer.
func (ds *DeviceState) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceState(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ds.ID))
	builder.WriteString("created_by=")
	builder.WriteString(fmt.Sprintf("%v", ds.CreatedBy))
	builder.WriteString(", ")
	builder.WriteString("updated_by=")
	builder.WriteString(fmt.Sprintf("%v", ds.UpdatedBy))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ds.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ds.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(ds.DeletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("device_id=")
	builder.WriteString(fmt.Sprintf("%v", ds.DeviceID))
	builder.WriteString(", ")
	builder.WriteString("delay=")
	builder.WriteString(fmt.Sprintf("%v", ds.Delay))
	builder.WriteByte(')')
	return builder.String()
}

// DeviceStates is a parsable slice of DeviceState.
type DeviceStates []*DeviceState

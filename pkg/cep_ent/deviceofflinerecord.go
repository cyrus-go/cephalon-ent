// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/device"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/deviceofflinerecord"
)

// 设备离线时间记录
type DeviceOfflineRecord struct {
	config `json:"-"`
	// ID of the ent.
	// 19 位雪花 ID
	ID int64 `json:"id,string"`
	// 创建者 ID
	CreatedBy int64 `json:"created_by,string"`
	// 更新者 ID
	UpdatedBy int64 `json:"updated_by,string"`
	// 创建时刻，带时区
	CreatedAt time.Time `json:"created_at"`
	// 更新时刻，带时区
	UpdatedAt time.Time `json:"updated_at"`
	// 软删除时刻，带时区
	DeletedAt time.Time `json:"deleted_at"`
	// 外键设备 id
	DeviceID int64 `json:"device_id,string"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeviceOfflineRecordQuery when eager-loading is set.
	Edges        DeviceOfflineRecordEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeviceOfflineRecordEdges holds the relations/edges for other nodes in the graph.
type DeviceOfflineRecordEdges struct {
	// Device holds the value of the device edge.
	Device *Device `json:"device,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DeviceOrErr returns the Device value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DeviceOfflineRecordEdges) DeviceOrErr() (*Device, error) {
	if e.loadedTypes[0] {
		if e.Device == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: device.Label}
		}
		return e.Device, nil
	}
	return nil, &NotLoadedError{edge: "device"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeviceOfflineRecord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case deviceofflinerecord.FieldID, deviceofflinerecord.FieldCreatedBy, deviceofflinerecord.FieldUpdatedBy, deviceofflinerecord.FieldDeviceID:
			values[i] = new(sql.NullInt64)
		case deviceofflinerecord.FieldCreatedAt, deviceofflinerecord.FieldUpdatedAt, deviceofflinerecord.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeviceOfflineRecord fields.
func (dor *DeviceOfflineRecord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case deviceofflinerecord.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			dor.ID = int64(value.Int64)
		case deviceofflinerecord.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				dor.CreatedBy = value.Int64
			}
		case deviceofflinerecord.FieldUpdatedBy:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_by", values[i])
			} else if value.Valid {
				dor.UpdatedBy = value.Int64
			}
		case deviceofflinerecord.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				dor.CreatedAt = value.Time
			}
		case deviceofflinerecord.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				dor.UpdatedAt = value.Time
			}
		case deviceofflinerecord.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				dor.DeletedAt = value.Time
			}
		case deviceofflinerecord.FieldDeviceID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value.Valid {
				dor.DeviceID = value.Int64
			}
		default:
			dor.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeviceOfflineRecord.
// This includes values selected through modifiers, order, etc.
func (dor *DeviceOfflineRecord) Value(name string) (ent.Value, error) {
	return dor.selectValues.Get(name)
}

// QueryDevice queries the "device" edge of the DeviceOfflineRecord entity.
func (dor *DeviceOfflineRecord) QueryDevice() *DeviceQuery {
	return NewDeviceOfflineRecordClient(dor.config).QueryDevice(dor)
}

// Update returns a builder for updating this DeviceOfflineRecord.
// Note that you need to call DeviceOfflineRecord.Unwrap() before calling this method if this DeviceOfflineRecord
// was returned from a transaction, and the transaction was committed or rolled back.
func (dor *DeviceOfflineRecord) Update() *DeviceOfflineRecordUpdateOne {
	return NewDeviceOfflineRecordClient(dor.config).UpdateOne(dor)
}

// Unwrap unwraps the DeviceOfflineRecord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dor *DeviceOfflineRecord) Unwrap() *DeviceOfflineRecord {
	_tx, ok := dor.config.driver.(*txDriver)
	if !ok {
		panic("cep_ent: DeviceOfflineRecord is not a transactional entity")
	}
	dor.config.driver = _tx.drv
	return dor
}

// String implements the fmt.Stringer.
func (dor *DeviceOfflineRecord) String() string {
	var builder strings.Builder
	builder.WriteString("DeviceOfflineRecord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dor.ID))
	builder.WriteString("created_by=")
	builder.WriteString(fmt.Sprintf("%v", dor.CreatedBy))
	builder.WriteString(", ")
	builder.WriteString("updated_by=")
	builder.WriteString(fmt.Sprintf("%v", dor.UpdatedBy))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(dor.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(dor.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("deleted_at=")
	builder.WriteString(dor.DeletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("device_id=")
	builder.WriteString(fmt.Sprintf("%v", dor.DeviceID))
	builder.WriteByte(')')
	return builder.String()
}

// DeviceOfflineRecords is a parsable slice of DeviceOfflineRecord.
type DeviceOfflineRecords []*DeviceOfflineRecord

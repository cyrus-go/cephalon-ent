// Code generated by ent, DO NOT EDIT.

package cep_ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/missioncategory"
	"github.com/stark-sim/cephalon-ent/pkg/cep_ent/predicate"
)

// MissionCategoryQuery is the builder for querying MissionCategory entities.
type MissionCategoryQuery struct {
	config
	ctx        *QueryContext
	order      []missioncategory.OrderOption
	inters     []Interceptor
	predicates []predicate.MissionCategory
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MissionCategoryQuery builder.
func (mcq *MissionCategoryQuery) Where(ps ...predicate.MissionCategory) *MissionCategoryQuery {
	mcq.predicates = append(mcq.predicates, ps...)
	return mcq
}

// Limit the number of records to be returned by this query.
func (mcq *MissionCategoryQuery) Limit(limit int) *MissionCategoryQuery {
	mcq.ctx.Limit = &limit
	return mcq
}

// Offset to start from.
func (mcq *MissionCategoryQuery) Offset(offset int) *MissionCategoryQuery {
	mcq.ctx.Offset = &offset
	return mcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mcq *MissionCategoryQuery) Unique(unique bool) *MissionCategoryQuery {
	mcq.ctx.Unique = &unique
	return mcq
}

// Order specifies how the records should be ordered.
func (mcq *MissionCategoryQuery) Order(o ...missioncategory.OrderOption) *MissionCategoryQuery {
	mcq.order = append(mcq.order, o...)
	return mcq
}

// First returns the first MissionCategory entity from the query.
// Returns a *NotFoundError when no MissionCategory was found.
func (mcq *MissionCategoryQuery) First(ctx context.Context) (*MissionCategory, error) {
	nodes, err := mcq.Limit(1).All(setContextOp(ctx, mcq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{missioncategory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mcq *MissionCategoryQuery) FirstX(ctx context.Context) *MissionCategory {
	node, err := mcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MissionCategory ID from the query.
// Returns a *NotFoundError when no MissionCategory ID was found.
func (mcq *MissionCategoryQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mcq.Limit(1).IDs(setContextOp(ctx, mcq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{missioncategory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mcq *MissionCategoryQuery) FirstIDX(ctx context.Context) int64 {
	id, err := mcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MissionCategory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MissionCategory entity is found.
// Returns a *NotFoundError when no MissionCategory entities are found.
func (mcq *MissionCategoryQuery) Only(ctx context.Context) (*MissionCategory, error) {
	nodes, err := mcq.Limit(2).All(setContextOp(ctx, mcq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{missioncategory.Label}
	default:
		return nil, &NotSingularError{missioncategory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mcq *MissionCategoryQuery) OnlyX(ctx context.Context) *MissionCategory {
	node, err := mcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MissionCategory ID in the query.
// Returns a *NotSingularError when more than one MissionCategory ID is found.
// Returns a *NotFoundError when no entities are found.
func (mcq *MissionCategoryQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = mcq.Limit(2).IDs(setContextOp(ctx, mcq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{missioncategory.Label}
	default:
		err = &NotSingularError{missioncategory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mcq *MissionCategoryQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := mcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MissionCategories.
func (mcq *MissionCategoryQuery) All(ctx context.Context) ([]*MissionCategory, error) {
	ctx = setContextOp(ctx, mcq.ctx, "All")
	if err := mcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MissionCategory, *MissionCategoryQuery]()
	return withInterceptors[[]*MissionCategory](ctx, mcq, qr, mcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mcq *MissionCategoryQuery) AllX(ctx context.Context) []*MissionCategory {
	nodes, err := mcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MissionCategory IDs.
func (mcq *MissionCategoryQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if mcq.ctx.Unique == nil && mcq.path != nil {
		mcq.Unique(true)
	}
	ctx = setContextOp(ctx, mcq.ctx, "IDs")
	if err = mcq.Select(missioncategory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mcq *MissionCategoryQuery) IDsX(ctx context.Context) []int64 {
	ids, err := mcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mcq *MissionCategoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mcq.ctx, "Count")
	if err := mcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mcq, querierCount[*MissionCategoryQuery](), mcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mcq *MissionCategoryQuery) CountX(ctx context.Context) int {
	count, err := mcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mcq *MissionCategoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mcq.ctx, "Exist")
	switch _, err := mcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("cep_ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mcq *MissionCategoryQuery) ExistX(ctx context.Context) bool {
	exist, err := mcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MissionCategoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mcq *MissionCategoryQuery) Clone() *MissionCategoryQuery {
	if mcq == nil {
		return nil
	}
	return &MissionCategoryQuery{
		config:     mcq.config,
		ctx:        mcq.ctx.Clone(),
		order:      append([]missioncategory.OrderOption{}, mcq.order...),
		inters:     append([]Interceptor{}, mcq.inters...),
		predicates: append([]predicate.MissionCategory{}, mcq.predicates...),
		// clone intermediate query.
		sql:  mcq.sql.Clone(),
		path: mcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MissionCategory.Query().
//		GroupBy(missioncategory.FieldCreatedBy).
//		Aggregate(cep_ent.Count()).
//		Scan(ctx, &v)
func (mcq *MissionCategoryQuery) GroupBy(field string, fields ...string) *MissionCategoryGroupBy {
	mcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MissionCategoryGroupBy{build: mcq}
	grbuild.flds = &mcq.ctx.Fields
	grbuild.label = missioncategory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedBy int64 `json:"created_by"`
//	}
//
//	client.MissionCategory.Query().
//		Select(missioncategory.FieldCreatedBy).
//		Scan(ctx, &v)
func (mcq *MissionCategoryQuery) Select(fields ...string) *MissionCategorySelect {
	mcq.ctx.Fields = append(mcq.ctx.Fields, fields...)
	sbuild := &MissionCategorySelect{MissionCategoryQuery: mcq}
	sbuild.label = missioncategory.Label
	sbuild.flds, sbuild.scan = &mcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MissionCategorySelect configured with the given aggregations.
func (mcq *MissionCategoryQuery) Aggregate(fns ...AggregateFunc) *MissionCategorySelect {
	return mcq.Select().Aggregate(fns...)
}

func (mcq *MissionCategoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mcq.inters {
		if inter == nil {
			return fmt.Errorf("cep_ent: uninitialized interceptor (forgotten import cep_ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mcq); err != nil {
				return err
			}
		}
	}
	for _, f := range mcq.ctx.Fields {
		if !missioncategory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("cep_ent: invalid field %q for query", f)}
		}
	}
	if mcq.path != nil {
		prev, err := mcq.path(ctx)
		if err != nil {
			return err
		}
		mcq.sql = prev
	}
	return nil
}

func (mcq *MissionCategoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MissionCategory, error) {
	var (
		nodes = []*MissionCategory{}
		_spec = mcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MissionCategory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MissionCategory{config: mcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(mcq.modifiers) > 0 {
		_spec.Modifiers = mcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mcq *MissionCategoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mcq.querySpec()
	if len(mcq.modifiers) > 0 {
		_spec.Modifiers = mcq.modifiers
	}
	_spec.Node.Columns = mcq.ctx.Fields
	if len(mcq.ctx.Fields) > 0 {
		_spec.Unique = mcq.ctx.Unique != nil && *mcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mcq.driver, _spec)
}

func (mcq *MissionCategoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(missioncategory.Table, missioncategory.Columns, sqlgraph.NewFieldSpec(missioncategory.FieldID, field.TypeInt64))
	_spec.From = mcq.sql
	if unique := mcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mcq.path != nil {
		_spec.Unique = true
	}
	if fields := mcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, missioncategory.FieldID)
		for i := range fields {
			if fields[i] != missioncategory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mcq *MissionCategoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mcq.driver.Dialect())
	t1 := builder.Table(missioncategory.Table)
	columns := mcq.ctx.Fields
	if len(columns) == 0 {
		columns = missioncategory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mcq.sql != nil {
		selector = mcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mcq.ctx.Unique != nil && *mcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range mcq.modifiers {
		m(selector)
	}
	for _, p := range mcq.predicates {
		p(selector)
	}
	for _, p := range mcq.order {
		p(selector)
	}
	if offset := mcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mcq *MissionCategoryQuery) Modify(modifiers ...func(s *sql.Selector)) *MissionCategorySelect {
	mcq.modifiers = append(mcq.modifiers, modifiers...)
	return mcq.Select()
}

// MissionCategoryGroupBy is the group-by builder for MissionCategory entities.
type MissionCategoryGroupBy struct {
	selector
	build *MissionCategoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mcgb *MissionCategoryGroupBy) Aggregate(fns ...AggregateFunc) *MissionCategoryGroupBy {
	mcgb.fns = append(mcgb.fns, fns...)
	return mcgb
}

// Scan applies the selector query and scans the result into the given value.
func (mcgb *MissionCategoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mcgb.build.ctx, "GroupBy")
	if err := mcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MissionCategoryQuery, *MissionCategoryGroupBy](ctx, mcgb.build, mcgb, mcgb.build.inters, v)
}

func (mcgb *MissionCategoryGroupBy) sqlScan(ctx context.Context, root *MissionCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mcgb.fns))
	for _, fn := range mcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mcgb.flds)+len(mcgb.fns))
		for _, f := range *mcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MissionCategorySelect is the builder for selecting fields of MissionCategory entities.
type MissionCategorySelect struct {
	*MissionCategoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mcs *MissionCategorySelect) Aggregate(fns ...AggregateFunc) *MissionCategorySelect {
	mcs.fns = append(mcs.fns, fns...)
	return mcs
}

// Scan applies the selector query and scans the result into the given value.
func (mcs *MissionCategorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mcs.ctx, "Select")
	if err := mcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MissionCategoryQuery, *MissionCategorySelect](ctx, mcs.MissionCategoryQuery, mcs, mcs.inters, v)
}

func (mcs *MissionCategorySelect) sqlScan(ctx context.Context, root *MissionCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mcs.fns))
	for _, fn := range mcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mcs *MissionCategorySelect) Modify(modifiers ...func(s *sql.Selector)) *MissionCategorySelect {
	mcs.modifiers = append(mcs.modifiers, modifiers...)
	return mcs
}
